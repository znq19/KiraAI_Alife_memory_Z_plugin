# Alife Memory Z · 长期记忆·Z

> 让每一段记忆都有来处，每一段故事都有归处。

一个给 KiraAI 用的**长期分层记忆**插件：把 Alife 的记忆系统移植改造过来，让 Bot 记得住、想得起、说得清，而且记忆全部可以在网页端热改和监控。

---

## 前言

这个插件核心设计学习和借鉴于 [Alife](https://github.com/BDFFZI/Alife) 项目的官方记忆系统。我一直很羡慕我朋友为他的桌宠真央做出的表现良好的长期记忆方案，记得不少，回忆也不少。虽然我技术很有限，但最后还是算终于做出来了。希望能对大家有帮助。让每一段回忆，都有出处；每一个故事，都有归处。

---

## 30 秒看懂它能做什么

| 能力 | 说明 |
| --- | --- |
| **记得住** | 聊天内容、图片/语音识别结果、Bot 自己的回复、工具查询结果都会变成记忆 |
| **想得起** | 每轮自动把相关记忆喂给模型；模型也能用工具主动去翻旧账 |
| **说得清** | 每条记忆都带**时间、来源会话、来源用户、存档 ID**，不会把别人的经历说成你的 |
| **改得动** | 网页端可以直接改摘要、改画像、改关系、改名字，改完立刻生效，不用重启 |

---

## 记忆是怎么工作的

### 1. 三层结构：原始经历 → 压缩存档 → 永久记忆

```
L0 原始记录（原文，永久保留：只剥协议外壳，人说的话一个字不少）
 │  同一会话攒到 50 条 → 压缩最早的 40 条（剩下 10 条原文留作尾巴）
 ▼
L1 摘要（带时间范围 + 子存档 ID）
 │  上层攒到 4 条 → 压缩最早的 3 条
 ▼
L2 … L8（最多 8 层）
 
L100 永久记忆（不参与自动压缩，只能由 Bot 主动 Memorize 或你在网页端添加）
```

- **层级只表示"被压缩了几次"，不代表重要程度**。
- 被压缩掉的原文**不会删除**，摘要里保留子存档 ID，随时可以顺着 ID 读回最初的对话。
- `Forget` 只是把记忆移出"常驻上下文"，原文依然读得到。
- 攒够阈值后**不是每次都触发**，而是按 `probability`（默认 0.4）概率启动，避免一直烧模型。

### 2. 每轮给模型什么

每轮对话，插件会在**本轮用户消息里、用户真正说的话之前**插入一个 JSON 记忆块。默认是**情境化注入**（`inject_mode=situational`）：

| 档位 | 内容 | 何时出现 |
| --- | --- | --- |
| 常驻 | 参与者、Bot 身份、人物/群名、约定与偏好类事实（`commitment` / `preference` / `profile`）、最近 1–2 条压缩存档 | 每轮 |
| 触发·提到人 | 消息**提到某个已登记的名字**（含消息里的昵称）→ 带回关于这个人的事实（含跨会话、跨用户） | 只在相关时 |
| 触发·提到词 | 消息内容与**事实内容词面匹配**（中文按 2 字切分）→ 带回命中事实，默认门槛 2 分，最多 `top_k` 条 | 只在相关时 |
| 触发·回忆词 | 命中「记得 / 之前 / 上次 / 曾经」→ 放宽一档（`top_k×2` 条），并追加一句「可按需检索存档」 | 只在相关时 |
| 存档 | 用整句话词面检索**其他会话**的存档摘要；默认**连已归档的旧记忆一起搜**，同分时常驻优先 | 每轮（`recall_scope` 非 `session` 时） |
| 按需 | 细节由 `SearchMemoryArchive` / `ReadMemoryArchive` 取 | 模型自己判断 |

这样既有「碰到谁、碰到哪个词就想起相关的事」的手感，又不会每轮把整库事实倒进上下文。软删（审计撤回 / 合并掉）的事实、冷归档与超龄淡出的旧存档永远不参与自动召回。想回到旧的全量行为，把 `inject_mode` 设成 `full` 即可。

事实只带**判断和追溯需要的字段**（类别、主体、内容、关系、重要度、最新一条来源指针 `src`、记录日期）；内部簿记（指纹、版本、合并标记、审计时间等）不再进入上下文。`reason` 只留给审计模型和网页端，不再注入给对话模型。

**还没压缩的原始对话（L0）默认不进这个块**——KiraAI 自己的上下文里本来就有最近若干轮，再注入一遍只会让上下文膨胀、还挤占提示词缓存。需要时模型用 `SearchMemoryArchive` 主动检索，或 `ReadMemoryArchive(id)` 按 ID 读回；想恢复旧行为可以打开 `inject_recent_raw`。

同时往系统提示里追加一段**固定不变的**工具使用规则。这两处都设置 `persist=False`——**不会写进 KiraAI 的聊天历史**。

### 3. 模型可以主动回忆（工具）

| 工具 | 作用 |
| --- | --- |
| `SearchMemoryArchive` | **找记忆 / 取记忆**：给 `ids` 按短码读回存档（含子存档、`include_content` 读原文）；给关键词/时间/层级搜索，默认只给"这个会话还没给过"的新内容 |
| `GetProfile` | **看人 / 看全局**：给 `subject`（实体 ID 或名字）看画像、名字历史、按类别事实与关系；`view=names` 只列现名与曾用名；都不给则返回总体统计与一批未见过的画像/偏好/约定 |
| `Memorize` | 保存**必须每轮在场**的约束或身份（可带 `category` / `importance`）；能被既有事实覆盖时会直接返回那条事实，不重复占常驻席位 |
| `CorrectMemory` | **维护记忆**（都要写 `reason`）：`update` 改字段、`merge` 合并重复、`delete` 软删、`restore` 恢复、`archive` 移出常驻、`refresh` 刷新昵称、`tidy` 请系统整理永久记忆 |

**同一会话只给新情报**：搜索默认排除这个会话已经给过的记忆，`already_seen` 告诉你跳过了多少；想看旧的可以用 `allow_seen=true` 或直接按 ID 读。

**检索结果的紧凑形态**（v2.7.0 起，为省 token 也是为了让模型少抄错）：

```json
{"ok": true, "total": 98, "next_page": 2, "already_seen": 12,
 "who": {"周武": "qq:769690776"},
 "items": [{"i": "k7f3a9", "t": "09-08 22:39", "s": "聊到师傅和星月的事…", "bot": 1, "arch": 1, "u": ["周武"]}]}
```

- `i` 是 **短码**（6 位），`ReadMemoryArchive` / `Forget` / `CorrectMemory` / `exclude_ids` 都同时接受短码和完整 ID —— 完整 ID 有 31~71 字符，抄错一位就查不到；
- `who` 把**完整账号整批只给一次**（名称 → 完整 ID），条目里用昵称引用，需要汇报或核对时从这里取；
- 默认值一律省略（`bot`/`arch`/`mem` 只在为真时出现），时间只留 `MM-DD HH:MM`；
- 摘要会做安全清洗：剥掉最外层 `<msg>`/`<text>` 包裹（**多段消息保留换行边界**）、压缩换行与多余空格；
  被折行插进中文词里的**孤立空格**会合并；**正文里字面出现的 `<text>`、`<` 等原样保留**；
  表情包描述与引用原文会截断（细节可用 `ReadMemoryArchive` 读全文）。
- **真实的空格不会被误伤**：① 连续被空白隔开的写法（`很 重 要`、`请 注 意`）视为刻意强调，原样保留；
  ② 实体表里**名字真的带空白的昵称**（`星 月`）会作为保护名单传入，渲染时不受空白归一影响；
  ③ 以上都只作用于「给模型看的那一份」与检索打分，**数据库原文一个字都不改**。
- 关键词检索同样做了空白归一：库里存着 `翅 膀`，用 `翅膀` 也搜得到，反过来也一样。

日志实测：两页共 60 条真实记录，返回体 **22,582 → 9,256 字符（−59%）**。

### 4. 后台整理（压缩 + 审计 + 永久记忆整理）

- **压缩**（压缩与合并模型）：按上面的层级规则把原始记录压成摘要；一次任务会连续排空积压，不会每轮都花钱。
- **永久记忆整理**（每 2 小时检查一次）：永久记忆超过**条数上限**（默认 10）或**字符预算**（默认 3000）时，
  后台请模型逐条判断 —— `keep`（继续常驻，可顺带修正类别/重要度）、`extract`（这条信息已能被事实覆盖 →
  提炼成事实后移出常驻）、`archive`(不再需要常驻)、`split`（一条里既有约束又有可提炼内容 → 拆开：约束留下、其余转事实）。
  **只归档不删除**：原文、版本、回收站都在，可一键"取回上下文"。条数上限触发时会一次排空积压。
- **召回去重**：每轮把相关事实带回来时，顺手做一次零成本的本地相似度判定——同主体、**同类别用 0.25**（与写入侧一致），
  **同主体、跨类别用 0.4**（同一件事被记成不同类别时靠它发现）；命中就标记并投递给后台合并队列
  （**不阻塞回复、不多花模型调用**），**本轮照常完整注入，从下一轮起被合并的那条才不再出现**。
- **合并判定有三个动作、没有 keep**：`merge`（合并）/ `relabel`（只统一类别）/ `drop`（删掉冗余的，软删可还原）；
  默认附上各条事实的**来源原文**作为判定依据（配置 `fact_merge_evidence` 可关掉省 token）。跨主体一律不合并。
- **写入时合并**（压缩与合并模型）：新事实入库时先在**同一会话 + 同一主体 + 同一类别**里找相似旧事实，达到阈值就**强制合并**（冲突以时间较晚的说法为准，模型输出不可用时用原文拼接兜底）；身份/偏好/关系类事实允许跨会话合并成一条全局记忆。合并完成前这条新事实不会进入检索，避免对话里同时看到重复。
- **审计**（审计模型）：定时检查事实是否重复/矛盾，提出修正或合并。默认**每 2 小时**一轮，每轮最多 `worker_count` 个会话、每个会话 ≤20 条；同一条事实距上次审计超过 `audit_recheck_days`（默认 7 天）才会再审，另有 `audit_daily_calls` 每日调用上限兜底——**没有待审事实就不会调用模型**。
- `dedupe` 任务用审计模型合并相似的永久记忆（检测到相似就合并，可配置关闭）。
- 所有任务都在后台队列里跑，默认 2 路并发，可热改；失败会保留原文并自动冷却重试。

### 5. 人物、画像与关系

- 名字来自新消息的昵称和会话标题，用**稳定 ID** 关联现名、曾用名、来源和观察时间；同名不会乱合并。
- 从记忆里聚合出**画像**（性格、偏好、关系、事实）和**关系三元组**，网页端有星图可以看。
- 「人物与群名」列表**不点开就有**：一行画像摘要（按重要度取前几条）+ 事实/关系/会话统计；点开可以看按类别分组的事实、关系连线和名字历史，每条都能编辑或**恢复历史版本**。
- Bot 也能直接查：`GetProfile("萤火")` 一步返回聚合画像，不必再自己拼 `MemoryNames` + `MemoryOverview`。
- 第三方插件（比如提醒插件）发的合成消息带占位昵称（`提醒任务所有者` 等），会被自动忽略，不会污染人物称呼。

### 6. 为什么不拖慢、不破坏缓存

- 系统提示里**只加一段固定文本**，每次请求逐字节相同；
- 所有会变化的内容（存档、事实、画像、时间、会话状态）都放在**本轮用户消息的最前面**，设置 `persist=False`；
- **不改写** KiraAI 的聊天历史和请求里已有的消息。

所以「系统提示 + 历史」这段前缀始终稳定，提示词缓存（prefix cache）能命中，只有最后一条消息是新的。

---

## 安装

1. 把本仓库内容（至少 `main.py` / `manifest.json` / `schema.json` / `web/` / 其他 `.py` 文件）放进 KiraAI 的 `data/plugins/alife_memory_z/`，或在插件管理里安装本仓库的 ZIP。
2. 安装依赖：`pip install -r requirements.txt`（KiraAI 自带 FastAPI 和 Pydantic，无需另装）。
3. 在 KiraAI 插件管理里启用，侧边栏会出现 **长期记忆·Z**。

> 核对过的 KiraAI 核心版本：**v2.34.1**（提交 `cad046f`）。没有设置 `core_version` 门槛，兼容性通过真实接口测试确认。

## 第一次使用（推荐设置）

1. 侧边栏 **偏好设置** → 确认 `启用` 和 `记录对话与感知` 打开。
2. `Bot可访问范围`：想让它跨群、跨私聊回忆就保持 `global`；只想在一个会话里回忆就选 `session`。
3. `压缩模型`：默认用快速模型即可；经常超时换更快的。
4. `审计模型`：默认用主模型，建议选理解准确的那个。
5. **同装会话合并/压缩类插件（KSM / ADS / Context Condensation）时，把 `上下文字符预算` 从 24000 调到 8000 左右**（原因见下方兼容性）。
6. 想省钱：`向量检索` 保持关闭（默认），它只影响语义搜索，关键词搜索不受影响。

## 配置速查

| 配置 | 默认 | 说明 |
| --- | --- | --- |
| `enabled` | 开 | 总开关，关掉后暂停记录、召回和后台整理，记忆保留 |
| `capture_enabled` | 开 | 记录新对话与感知结果 |
| `bootstrap_seed` | `auto` | 首次遇到旧会话是否把 KiraAI 已有历史抄进记忆库；`auto` 在检测到会话合并/压缩插件时跳过，`always` 始终播种，`off` 从不播种 |
| `auto_inject` | 开 | 每轮自动注入相关记忆 |
| `inject_recent_raw` | 关 | 是否把未压缩的最近原文也塞进记忆块。默认关：宿主上下文里已有，重复注入只会膨胀 |
| `inject_mode` | `situational` | 注入形态：`situational` 常驻只放约定/偏好/画像/最近摘要，提到人或触发词才带回相关记忆；`full` 是旧的全量行为 |
| `threshold` / `batch_size` | 50 / 40 | 攒够多少条开始压缩、每次压缩多少条（`batch_size` 必须小于 `threshold`；差额 = 压缩后留下的原文尾巴，默认 10 条） |
| `probability` | 0.8 | 满足阈值时启动压缩的概率（检查点：Bot 每次回复后 + 调度器每 30 秒一次，所以低于 1 也只是晚几十秒） |
| `max_level` | 8 | 最多压缩几层 |
| `compress_model` | 空=用默认快速模型 | **压缩与合并模型**：压缩归档 + 写入时事实合并 |
| `audit_model` | 空=用默认主模型 | **审计模型**：审计纠错 + 永久记忆合并 |
| `audit_enabled` / `audit_interval` | 开 / 7200 秒 | 后台审计开关与间隔（默认 2 小时） |
| `audit_recheck_days` | 7 | 同一条事实距上次审计超过这么多天才再审（0=每次都可能再审） |
| `audit_daily_calls` | 24 | 审计模型每日调用上限，用完停到第二天（0=不限） |
| `compress_persona` / `audit_persona` | 开 / 关 | 压缩与审计请求是否带 Bot 人设。人设很长（数千字）时，关掉能省下每次调用的大头 |
| `fact_merge_enabled` / `fact_merge_threshold` | 开 / 0.25 | 写入时合并开关与触发阈值 |
| `fact_merge_soft_chars` / `fact_merge_max_chars` | 80 / 150 | 合并后事实的提示词字数与硬上限 |
| `fact_merge_soft_reason_chars` / `fact_merge_reason_chars` | 15 / 40 | 合并理由的提示词字数与硬上限 |
| `fact_merge_batch_clusters` | 5 | 一次模型调用最多合并几组 |
| `fact_merge_prompt` / `record_merge_prompt` | 内置 | 两个合并提示词，可用 `{content_max}` / `{reason_max}` 占位符 |
| `cross_session_merge` | 开 | 身份/偏好/关系类事实允许跨会话合并为全局记忆 |
| `merge_pending_hide` | 开 | 检测到重复的新事实在合并完成前不参与检索 |
| `record_merge_soft_chars` / `record_merge_max_chars` | 500 / 16000 | 永久记忆合并的字数（提示词 / 硬上限） |
| `record_merge_soft_reason_chars` / `record_merge_reason_chars` | 15 / 60 | 永久记忆合并理由的字数 |
| `profile_summary_count` | 3 | 人物卡片上直接显示的画像要点条数 |
| `worker_count` | 2 | 后台并发任务数（1–4） |
| `context_chars` | 24000 | 每轮注入记忆的总字符预算（同装其他上下文插件建议 8000） |
| `token_warning` | 120000 | 上下文估算超限时打警告 |
| `recall_scope` | `global` | 可访问范围：`session` / `linked` / `global` |
| `top_k` | 5 | 每轮最多自动找回几条相关跨会话存档；情境化模式下也决定「提到某个人时带回他几条事实」 |
| `fact_recall_min_score` | 2 | **事实内容匹配门槛**：中文按 2 字切分，命中任一双字片段 = 2 分；调高更干净但会漏（只提到「师傅」两字就会被挡掉），填 0 关闭内容匹配 |
| `search_active_only` | 关 | 关闭（默认）时检索连已归档的旧记忆一起搜，被动召回更全；开启则只搜常驻，归档需 `include_archived=true` |
| `cold_after_days` | 180 | 多久没用到就淡出检索（只影响检索；冷归档只能按 ID 读） |
| `memorize_cover_check` | 开 | 保存永久记忆前先查是否已被事实覆盖；能覆盖就不重复常驻 |
| `fact_merge_cross_threshold` | 0.4 | 同主体、跨类别的重复判定阈值（同类别仍是 0.25） |
| `fact_merge_evidence` | 开 | 去重判定是否附来源原文（更准但费 token，可关） |
| `permanent_tidy_enabled` | 开 | 永久记忆超条数/超预算时后台整理（keep / extract / archive / split） |
| `permanent_cap` | 10 | 常驻永久记忆条数上限，超过即触发整理（并一次排空积压） |
| `permanent_budget_chars` | 3000 | 常驻永久记忆字符预算，超过即触发整理 |
| `permanent_tidy_batch` | 10 | 一次整理最多交给模型几条 |
| `permanent_tidy_days` | 14 | 同一条多久内不重复整理（天） |
| `permanent_dedupe` / `dedupe_force_merge` | 开 / 开 | 永久记忆去重；检测到相似就合并 |
| `semantic_enabled` | 关 | 是否启用向量检索（会产生 embedding 费用） |
| `auto_migrate` / `mutual_exclusion` | 开 / 开 | 自动迁移旧记忆插件（后台执行、源文件未变则跳过）、并停用冲突插件（原文件只读保留） |
| `boot_enabled` / `boot_replay_seconds` | 开 / 90 | 打开面板的载入动画与重播冷却 |
| `proactive_enabled` | 关 | 定时主动感知：让 Bot 依据记忆主动关心用户（默认不发消息） |
| `proactive_interval` / `proactive_jitter` | 3600 / 0 | 基础间隔（最短 60 秒，最长 7 天）与随机偏移：实际间隔 = 间隔 + 0~偏移 的随机值，0 = 固定间隔 |
| `proactive_min_sessions` / `proactive_max_sessions` | 1 / 0 | 每轮挑几个会话：最少/最多，0 = 不限（全部触发）；两者相等就是固定个数，不随机 |
| `proactive_rotate` | 关 | 关=每轮纯随机抽取会话；开=优先挑最久没被触发过的（同龄随机），避免长期没轮到 |

所有参数都可以在网页端改，**原子保存、即时生效**，不用重启。

## 网页端（侧边栏）

| 页面 | 用途 |
| --- | --- |
| 概览 | 记忆/事实/画像/关系数量、迁移与互斥状态、载入动画 |
| 存档管理 | 按会话、用户、类型、标签、层级筛选；读原文、改摘要、软删除 |
| 画像与关系 | 画像卡片、关系星图、证据回溯 |
| 后台任务 | 手动排队压缩/审计/合并/重建索引；每条任务可点「明细」看这次处理了哪几条并直接编辑 |
| 回收站 | 已删除的事实/存档与冷归档，按维度查看、搜索、还原或编辑 |
| 偏好设置 | 所有配置项；文案类字段旁边有「恢复默认」 |

**存档管理的会话筛选**：选中某个会话后默认**只显示该会话自己的记忆**。旧插件迁移来的全局记忆、以及跨会话共享给本会话参与者的记忆，需要勾选「含跨会话记忆」才会一并列出（此时它们会带 `跨会话` 标签）。

**搜索的两种语义**：存档管理页搜的是**存档摘要**（内容匹配）；画像与关系页勾选「按内容搜」后，输入框变成**事实内容**搜索，配合维度筛选可以缩小到某一类。

**星尘特效**：页面背景有星点与流星（同一色系随机变色），顶栏 `✦` 可关闭；系统开启「减少动态效果」时会自动停用。

**人物与群名的批量拉取**：名字页的「批量拉取姓名」按钮会先弹二次确认——「只补没有名字的」只处理缺名的号码、已有名字一律跳过；「全更新」则连已有名字一起用平台当前昵称更新，旧名保留在曾用名里、可一键恢复。

**三个维护按钮**（后台任务页底部）：

- **清理历史工具记录**：把工具结果压成 240 字预览，并移除"读自己记忆"的回声记录（原文保留）；
- **修复被改写的昵称**：第三方插件把昵称写成 `提醒任务所有者` 之类时，恢复上一个正常称呼；
- **清理历史播种记录**：删除"安装插件时抄进来的旧对话"，与会话合并插件同装时尤其有用（软删除，原文保留，且清理后不会重新播种）。

> 2.3.0 起，如果检测到会话合并/压缩插件、且库中存在**来源无法确认**的播种记录（旧版本遗留），概览页会自动弹一条提示条，提供「去清理」和「保留，不再提醒」——不会自动删除任何记忆。

## 数据在哪 / 备份

- 数据库：`data/plugin_data/alife_memory_z/alife-v2.sqlite3`
  （KiraAI 的插件**代码**在 `data/plugins/`，插件**数据**在 `data/plugin_data/`，两者分离，升级/重装插件目录不会丢记忆）
- 升级旧版时会先用 SQLite 在线备份保存 `alife-v2.pre-v3.sqlite3`，备份失败会停止初始化。
- 网页端「导出完整记忆」可以导出包含记录、父子关系、事实、修改历史和迁移凭据的完整 JSON；数据库本身也可以用任何 SQLite 工具打开。

## 与其他插件一起用

| 插件 | 会不会冲突 | 说明 |
| --- | --- | --- |
| KSM 会话合并 `kira_session_merger` | 不冲突 | 它注入跨会话**原始对话**，我们注入跨会话**记忆档案**，互补 |
| ADS 自动压缩 `auto_delete_session` | 不冲突 | 它压缩 KiraAI 自己的会话历史，我们的记忆库是独立副本 |
| Context Condensation | 不冲突 | 它写回摘要块、明确不动系统提示；实测不会误读我们的记忆块 |

三点要知道：

1. **不会互相禁用**：我们的互斥名单只有两个老记忆插件（`kira_plugin_simple_memory`、`kira_plugin_kiraos`）。
2. **旧历史播种保护（2.3.0 新增）**：KSM 会把别的会话的对话合并进当前请求，如果这时把整段请求历史抄进记忆库，就会"把别人的经历记成你的"。所以默认 `bootstrap_seed=auto`：**检测到 KSM / ADS / Context Condensation 启用时跳过播种**，只记装插件之后的内容。
   - 检测到合并插件、且库里有**旧版本遗留**、来源无法确认的播种记录时，概览页会提示你核对（可一键保留不再提醒），**不会自动删数据**。
3. **上下文预算互相看不见**：这三个插件的压缩阈值只统计 KiraAI 自己的历史，看不到我们注入的记忆块（最大 24000 字符）。同装时建议把 `context_chars` 调到 **8000 左右**，或给 ADS 的 `max_tokens` 留出余量。

> 另外，KSM 和 ADS 默认都会自动禁用 Context Condensation（`disable_ccs`）——这是它们三者之间的策略，与本插件无关。

## 常见问题

**Q：为什么它好像不记得？**
依次检查：`enabled` 和 `capture_enabled` 是否打开 → `recall_scope` 是否够大（跨群回忆要 `global`）→ `context_chars` 是否被压得太小 → 这段记忆是不是被 `Forget` 或冷归档了（用 `SearchMemoryArchive(include_archived=true)` 或按 ID 读）。

**Q：会花很多钱吗？**
压缩和审计会调模型，但只在攒够阈值后按概率触发；向量检索默认关闭。想更省：把 `probability` 调低、`audit_enabled` 关掉、用便宜的快速模型。

**Q：它会改 KiraAI 自己的聊天记录吗？**
不会。我们只读不写宿主历史，记忆存在自己的 SQLite 里。

**Q：和 KSM 一起用，第一次装上为什么没继承旧对话？**
这是 2.3.0 的播种保护：KSM 在改写上下文时，历史里可能混着别的会话，直接抄进来会串味。`bootstrap_seed=always` 可以强制播种，或在没装 KSM 时先播种。

**Q：旧记忆插件的数据怎么办？**
默认开启安全迁移：只读接入旧默认记忆的 `data/memory/core.txt` 和 KiraOS 的记忆文件，校验后导入，**原文件一律保留**；迁移成功后才停用冲突插件，失败则暂停自身并保持旧插件启用。

迁移在**后台执行**，不会卡住插件加载：启动后先在后台跑，期间记忆功能暂停（`migration_blocked`），完成后自动恢复，状态在概览页可见。如果旧记忆源文件没有变化、也没有旧插件在运行，启动时会**直接跳过扫描**（只 stat 文件、不解析），不会每次都重读一遍。

## 开发验证

```bash
python -m pip install -r requirements.txt pytest pytest-asyncio
python -m pytest tests -q                 # 单元测试（不需要核心）
KIRA_CORE=/path/to/KiraAI python -m pytest tests -q   # 真实核心接口测试
python tools/web_audit.py                 # 前端静态审计：属性读写配对、绑定与读取一致、接口与路由一致
```

`tools/web_audit.py` 专门拦截「属性名/接口路径对不上」这类只在浏览器里才发作、且要点到某个按钮才暴露的问题（v2.5.7 / v2.5.9 两次 404 都属于这一类），测试里也会调用它。

真实核心测试使用 KiraAI 的事件、请求、响应与插件注册类，模型结果由确定性夹具提供。测试详情见 [验证记录](docs/VERIFICATION.md)。

## 来源与许可

依据 BDFFZI/Alife 的 `MemoryService.cs`、`MemoryManager.cs`、`MemoryStorage.cs` 及层级回归测试做行为移植。保留本仓库 **AGPL-3.0** 许可证。名称与图标来自 znq19 的 Z 版；不冒称 Alife 或 KiraAI 官方插件。原作者与提交定位见 [来源说明](docs/PORTING.md)。

---

## 致谢

感谢 **半点星光** 和 [Alife](https://github.com/BDFFZI/Alife) 项目：没有群主和 Alife，也没有机会让我看到小贱——我的第一个 bot 如潮水般涌回的记忆。那份感动支持着我这次再试试做完。

感谢 [KiraOS](https://github.com/LyaQanYi/KiraOS_Plugin) 的参考：这是 KiraAI 项目首个也是第三方记忆插件，也是一直以来社区的标杆，我们一起接力下去，陪伴 Kira 记忆增长。

**特别感谢 [海葵（@atsmoe）](https://github.com/atsmoe)！** 没有海哥热心甚至带病的免费帮我操作和提供 GPT-6 重构最初的代码石山，我绝对坚持不到最后。非常感谢你！以后 bot 实体化了，第一个送你透。

---

<details>
<summary><b>📝 更新日志（点击展开）</b></summary>

### v2.16.4 (2026-09-12) — 修「每次压缩都失败」：紧凑模板写了契约里没有的字段 🚑

**线上现象**：分层压缩**没有一次成功** —— 全部 `structured_output_rejected · <field>: extra_forbidden`
（两个字段 ✗）

**根因（我的错）**：v2.16.0 手写 compress 的紧凑声明时，我把**输入 payload 的字段**
（`range`、`records`）当成了**输出字段**写进模板 ✗
而 `Compression` 契约其实**只有两个字段**：`summary` + `facts` ✓
→ 模型照着模板照做，多给两个键 → 每次都被拒 ✗✗

**修**：紧凑声明改成只列真实字段（`summary` + `facts`）✓
并加一句直白的提醒：「**只有这两个顶层键**，不要回写输入里的 range/records/names 等字段」✓

**测试缺口（这才是关键）** ✗：v2.16.0 的一致性测试只查"必填字段有没有**漏**"，
查不出"**凭空多写了字段**" ✗ → 新增
`test_compact_schema_does_not_invent_fields`：把模板里出现的每个键都拿去和契约对照，
只要有一个不在契约里就红灯 ✓✓（这条能直接拦住本次事故 ✓）

**顺带说明**：我一度想"用紧凑声明时强制多留一次重试"，但**撞了 3 个既有可靠性测试** ✗
（它们锁定了重试预算的语义 ✓）→ 已回退 ✓ 不顺手改预算语义 ✓

测试：默认 334 passed 14 skipped；KIRA_CORE 384 passed；web_audit 0。

### v2.16.3 (2026-09-12) — 永久记忆：整理与去重联动 + 不超重也定期体检 🧹

**问题（用户发现）**：永久记忆的「整理（tidy）」只在**超重**时才被触发
（条数 > `permanent_cap` 或字符 > `permanent_budget_chars`）✗
→ 一个会话长期稳定在 cap 之下时，**永远不会被整理** ✗
里面"其实该提炼成事实 / 已过时 / 表述冗余"的条目会一直每轮吃 token ✗

（顺带澄清：`permanent_tidy_days` 原来只是"整理时**挑哪些记录**"的 cutoff ✗
不是"多久整理一次" —— 现在它同时承担后者 ✓）

**三处改动**

| # | 触发 | 条件 |
|---|---|---|
| ① | **dedupe 完成后跟一次 tidy** | 本次 `merged > 0`（**真的合并了**才跟，避免空转 ✗）|
| ② | **注入侧超重时**（现有）| 排 tidy ✓ **同时排一次 dedupe** ✓（超重往往是重复项堆出来的，顺手清老根 ✓）|
| ③ | **定时器周期体检**（新增）| 不超重、但存在 `tidy_at` 早于 `permanent_tidy_days`（14 天）的永久记忆 → 也排 tidy ✓ |
| ④ | **写下永久记忆后**（新增）| Bot 每次 `Memorize` 成功后**直接排一次 tidy** ✓（哪怕没触发去重 ✓）——新开关 `permanent_tidy_on_write` **默认开** ✓ |

④ 的开销很小：刚写入的那条本来就是"待整理项" ✓，旧记录在 `permanent_tidy_days` 内会被跳过 ✓
所以实际工作就是"把刚记下的这条过一遍"（判断该不该常驻 / 能否提炼成事实）✓

**为什么①的顺序重要**：先合并、再整理 → tidy 看到的是**更小更干净**的集合 ✓
提炼/移出的判断更准 ✓ 输入 token 也更少 ✓（反过来会让两者重复处理同一批 ✗）

**防乒乓**：①只在"有改动"时跟 ✓ ②只在超重信号下排 ✓ ③不反向叫 dedupe ✗
加上 `jobs` 表按 `kind+sid` 去重 ✓ 不会循环 ✓

**配置**：①②③ 复用现有开关 ✓；④ 新增 `permanent_tidy_on_write`（默认开 ✓）
（`permanent_tidy_days` 语义扩展为"至少多久整理一次"，设置页说明同步 ✓）

**测试**：默认 332 passed 14 skipped；KIRA_CORE 382 passed；web_audit 0
新增 `tests/test_tidy_dedupe_link.py`：
① `permanents_need_tidy` 的四种情形（从没整理过 / 刚整理过 / 超过 14 天 / 关掉周期）✓
② 三处联动都在代码里（静态检查，防以后被改掉 ✗）

### v2.16.2 (2026-09-12) — 修「extra_forbidden」频繁失败（紧凑 schema 漏了白名单）🔧

**线上现象**：分层压缩频繁 `structured_output_rejected · <field>: extra_forbidden` ✗

**根因**：v2.16.0 把 schema 换成手写紧凑声明时，**丢了一条关键约束** ✗ ——
自动 schema 里带 `additionalProperties: false`（= 不许加字段 ✓），我的紧凑版没写这句 ✗
而契约是 `extra="forbid"`（严格 ✓）→ 模型多塞一个键（notes/explanation 之类）就整条被拒 ✗

**修**：三个紧凑块（compress / fact_merge / audit）统一补一行
**「字段白名单：只允许上面出现过的键，多任何一个都会被拒。」** ✓

**没做（特意回退）**：我一度改成"解析时静默剪掉多余的键" ✗ —— 但那与既有设计冲突：
现在的设计是**明确拒绝 + 把出问题的键名提示给模型**（`test_output_diagnostics` 一直在保护这条 ✓
"诊断不回声私密内容 + 提示允许的枚举/关系嵌套"）✓ 静默丢弃会让模型继续乱写 ✗ 也让诊断失效 ✗
→ 只修**提示词层**（根因 ✓），解析层保持严格 ✓；加上原有的"连续被拒两次 → 退回完整自动 schema"兜底 ✓

新增测试：三个紧凑块必须含「字段白名单」✓（防以后再漏）

测试：默认 330 passed 14 skipped；KIRA_CORE 380 passed；web_audit 0。

### v2.16.1 (2026-09-12) — 修轮换槽位：档案与事实的状态串号（线上 KeyError）🚑

**线上报错**：`on_request ... "t": short_time(r["end"] or r["start"])` → `KeyError: 'end'` ✗

**根因（我的设计缺陷）**：v2.15.0 的轮换槽位把「档案」和「事实」**共用同一份状态** ✗
—— 两边的批次混在一起，于是某一轮把**事实行**补进了档案列表 ✗
档案渲染要读 `r["end"]` → 事实行没有这个字段 → **每个请求都抛异常** ✗（记忆注入整体失效）

**修**：状态按**槽位**分开（`state["slots"]["archive"]` / `["fact"]` ✓）
- `rotation_extras(..., kind)`、`rotation_needs_batch(sid, kind)` 都带 kind ✓
- 反馈 `rotation_feedback` 遍历所有槽位 ✓（各自判定命中/留满）
- **内存计数快照**（`shown`/`used`）仍**共用一份** ✓（那是排序依据，与槽位无关 ✓）
- seen 窗口仍然共用 ✓（按你之前的决定 ✓）

**新增回归测试** `test_archive_and_fact_slots_do_not_mix` ✓：
两个池子各挑一批 → 档案槽位拿到的行必须有 `summary`、事实槽位必须有 `content` ✓
（这个 bug 正是"没测两个槽位并存"漏掉的 ✗）

测试：默认 329 passed 14 skipped；KIRA_CORE 379 passed；web_audit 0。

### v2.16.0 (2026-09-12) — 成本优化：模板瘦身（省 token、不丢信息、不降质量）💰

按"钱、速度、质量都要"的目标做了一轮体检与瘦身。**先量账**：

| 用途 | 指令 | 契约 schema | 每次固定开销 |
|---|---|---|---|
| 压缩 | 539 | **1525** | 2526 字 ✗ |
| 审计 | 403 | **1318** | 2183 字 |
| 事实合并 | 683 | 1073 | 2218 字 |

一次调用光"开场白"就 1.4k~2.5k 字 ✗，而正文可能才几千字 → **摊销极差** ✓ 这是主因。

**改动 1：schema 瘦身（省 740~980 字/次）**
pydantic 自动 schema 里的 `$defs` / `additionalProperties` / `maxLength:16000` 这些**机器语法**
对模型没有行为价值（人话约束本来就在 `COMMON_INSTRUCTION` 和各用途指令里 ✓），换手写紧凑声明：

| 用途 | 旧 | 新 | 省 |
|---|---|---|---|
| compress | 1525 | 602 | **923** |
| fact_merge | 1073 | 335 | **738** |
| audit | 1318 | 380 | **938** |

紧凑声明里**一个不少**地保留：结构、字段名、**必填清单**、枚举、**行为性上限**，
并新增一行**「常见错误（会被拒）」**——把我们线上真踩过的坑写进去
（predicate/object 平铺、source_ids 编造、content 为空、target_id 不在输入里…）✓
→ 所以这一版**比原来更稳**，不是纯砍 ✓
**兜底**：同一调用**连续被拒两次 → 自动退回完整自动 schema** ✓ 最坏情况 = 旧成本 ✓

**改动 2：规则块精简（省 ~350 字/轮）**
`MEMORY_RULES` 1103 → **753 字** ✓ 砍掉的是**工具使用手册的细节**（工具的 function schema 里
本来就带完整说明 ✓ 重复两遍纯浪费 ✗）；**字段词典与关键约束一个不删** ✓
（`next_batch`/`needs_review`/`src`/`sp`/`rec`/`t2`/四个工具名 ✓ 有测试钉着 ✓）

**改动 3（确认后不动）：稳定前缀本来就在最前面** ✓
`model_call` 的组装顺序是 `system = COMMON + 指令 + schema` ✓、`user = payload` ✓
→ system 部分**逐字节稳定** ✓ provider 的前缀缓存本来就能吃到它 ✓ **无需改动** ✓

**本轮按你的意见不动的**：审计证据按需截断 ✗（审计的价值就是有据可查 ✓ 削它伤质量 ✓）；
再压 `src`/`sid`/`by` 等字段名 ✗（省 20~30 字/条不值得 ✓）；正文/时间区间/摘要一律不截 ✗

**改动 4：`u` 只留 ID + 顶层 `names` 表（v2.16.0 追加）**
记录里的 `u` 从 `qq:769690776(周武)` 改成**纯 ID** ✗→✓，名字只在 payload 顶层 `names` 表给一次 ✓
`COMMON_INSTRUCTION` 同步改为「`records[].u` 是实体 ID 列表，名字看 names 表」✓
**实测省**：2 人参与的 40 条批省 **284 字**（我原先估 1.2k 是虚的 ✗ 参与者越多省得越多 ✓ 6 人批约省 1k）

**改动 5：轮换槽位省掉多余查询（v2.16.0 追加）**
只在**真要换批**时才查事实候选 ✓；"继续留批"的轮次直接复用上批 ✓ → 每 3 轮省 2 次查询 ✓
并且：**配置一变（门槛/条数/命中阈值）上批立刻作废重挑** ✓ —— 否则调高门槛后上批还会赖着不走 ✗
（这个边界是**测试抓出来的** ✓ `test_recall_threshold_is_configurable`）
同时把轮换事实查询的门控对齐主路径（`query` 非空 + 未超预算 ✓），避免绕过门槛 ✗

**未做**：prewarm 覆盖轮换挑选（需要注入上下文，收益最小 ✓ 留作后续）

**测试**：默认 328 passed 13 skipped；KIRA_CORE 377 passed；web_audit 0
新增 `tests/test_compact_templates.py`：
① 紧凑声明必须覆盖契约**全部必填字段**（**当场抓到 audit 漏了 relations 三个字段** ✓✓）
② 真瘦（<自动 schema 的 70%）且必须含"必填/常见错误"
③ 兜底存在 + 提示词组装支持字符串 schema
④ 规则块关键 token 一个不少 + 体积上限（防止以后涨回去 ✗）

### v2.15.0 (2026-09-12) — 轮换召回槽位：把"相关但从没露过脸"的记忆轮进来 🔄

**问题**：召回打分是确定的 → **第 6 名永远见不到天日** ✗ —— 同一批"最相关"的每轮都赢，
而"同样相关、只是排在后面"的记忆一次都不会被带进上下文 ✗

**做法**（用户主导的设计）：主槽位不变 ✓（最相关的照旧），**额外槽位**只在
「**同样过门槛、但还没被召回过**」的候选里挑 ✓ —— 门槛与主召回**完全相同** ✓
（所以它们一样是相关的、看起来也不突兀，**不加任何标记** ✓），池子空了就**留空** ✗
（绝不降门槛拿不相关的凑数 ✗）

**反馈驱动换批**（不是每轮乱换）：
- 判定"被用到"：这轮**回复**里出现该记忆的独特词元（QQ 号/长数字）→ 命中 1 个即算 ✓；
  否则按词元重合数 ≥ `rotate_min_hits` ✓
- **用到了 → 下轮换批** ✓；一直没用 → 最多留 `rotate_keep_rounds`=3 轮 ✓ 然后也换 ✓
- 下场的进 `rotate_cooldown_rounds`=10 轮冷却 ✓，避免来回抖动
- **学习排序**：从没展示过的优先 ✓ → 用过/展示比例高的次之 ✓ → 展示次数少的再后 ✓
  （所以"总被无视"的记忆会自然沉底 ✓）

**覆盖范围**：档案与事实**都启用** ✓，共用一套参数 ✓，共用同一个 seen 窗口 ✓（与工具去重一致 ✓）

**缓存**：不影响 ✓ —— 记忆块本来就在**请求末尾**（`messages.append(user_prompt)` ✓ 框架侧 ✓），
前缀（system + 历史）照旧可缓存 ✓；槽位只多花它自己那几条的 token ✓

**配置**：`rotate_enabled`（默认**开** ✓）、`rotate_count`=3、`rotate_keep_rounds`=3、
`rotate_min_hits`=2、`rotate_cooldown_rounds`=10 ✓（宿主设置页的人话说明同步补齐 ✓）

**顺带修**：`setting_help.py` 里 `threshold`/`batch_size`/`probability` 的说明还写着旧的
100/70/0.4 ✗（2.14 改过默认值了）→ 已同步为 50/40/0.8 ✓

测试：默认 323 passed 13 skipped；KIRA_CORE 372 passed；web_audit 0。
新增 `tests/test_rotation_slot.py`：挑选顺序（没展示过 → 用过比例 → 展示次数）、
池空留空、留批、命中即换、留满即换、seen 与冷却过滤、命中判定（数字串/词元重合）。

### v2.14.1 (2026-09-12) — 修存量回填"没有日志"（计数漏了事实那条）🔧

用户反馈：启动后**看不到** `存量时间/发言人回填：N 条记录` 这行 ✗

**两个我自己的 bug**（不是数据问题）：
1. `backfill_time_provenance()` **只返回"补了发言人的记录条数"** ✗ —— 事实那条
   （`event_at/event_end`）的 rowcount **被丢掉了** ✗ → 如果这批里没有一条记录能判定发言人，
   调用方拿到 0 → `if total:` 不成立 → **什么都不打** ✗（明明事实已经补完了）
2. 调用方 `if not filled: break` 用的是"**补成功**的条数" ✗ → 一批全是"扫到但判定不出来"
   的记录时，循环**提前退出** ✗ → 后面的行这一轮就不扫了

**修**：
- 回填改为返回 `{facts, records, scanned, unresolved}` —— facts 必须计 ✓
- 循环改成按 `scanned` 判断是否继续 ✓
- **无论如何都打一行**（哪怕全是 0）✓：不然"没跑"和"跑完没东西可补"分不清 ✗
  ```
  [记忆·Z] 存量时间/发言人回填：事实 N 条、记录 M 条（另有 K 条群聊老记录判定不出是谁说的，新记录不受影响）
  ```
- 新增**接线测试**：直接调启动时那个后台任务 `build_search_index()`，断言它真的把
  存量事件时间补上了 —— 这个 bug 就是因为只测了方法本身、没测接线 ✗

测试：默认 320 passed 11 skipped；KIRA_CORE 367 passed；web_audit 0。

### v2.14.0 (2026-09-12) — 时间来源与发言人：终于能回答「这是什么时候、谁说的」🕰️

用户问：每条事实/记忆都有"被记下来的时间"吗？「星月师傅**昨天**想吃蛋糕」这种能定位到哪天吗？
合并后的事实、存档呢？还有：Bot 读原文时**看不到是谁说的** ✗

**查证结果**：时间其实**存着**，但**给模型的那份缺了一半**，而且两套语义混在一起 ✗
- `records.start/end` = 消息时间（精确到秒）✓ 存档注入/工具都带 `t` ✓
- `facts.created` = **入库时刻**（`time.time()` ✓）但老库回填时写的是**来源记录的事件时间** ✗ 两种含义 ✗
- 事实的注入**有** `t`（取 `created` ✗ 于是"什么时候"答的是"什么时候整理的" ✗），**没有区间** ✗
- 合并事实**不更新时间** ✗ → 多个时间点"塌成一点" ✗
- 抽取时**不换算相对时间** ✗ → "昨天"写进事实就永久失真 ✗✗
- 存档记录只有一个 `users`（= **可见范围**）✗ 没有"发言人" ✗ → 群聊里分不清谁说了哪句 ✗

**改动**
| # | 内容 |
|---|---|
| 1 | `facts` 新增 `event_at`/`event_end` = **事件时间区间**（由来源记录现算）；`created` 固定为**入库时刻** |
| 2 | 注入事实的 `t` 改用**事件时间**；跨天给 `t2` 区间；"发生"与"记录"差得远时附 `rec`（入库日期） |
| 2b | **跨年一律带年份**：事实用 `short_day`、存档用 `short_time`，**同年省年份、跨年给 `YYYY-`** —— 模型虽然知道"现在"，但看到「11-16 10:33」会当成今年 ✗（`full_time` 用于压缩输入，本来就带年份 ✓ 所以"昨天→绝对日期"能算对）|
| 3 | 合并事实取时间**并集**（不再塌成一点）；重做合并（还原）也一并恢复 |
| 4 | `records` 新增 `speaker` = **这条是谁说的**（显示名）；工具输出新增 `sp` 字段，与 `u`（可见范围）区分 |
| 5 | 提示词加硬规则：**把「今天/昨天/前天/上周」按记录时间换算成绝对日期**再写进摘要与事实 |
| 6 | 规则块补上 `t`/`t2`/`rec`/`sp` 的说明，并写明"要精确到分钟就把 `src` 当 id 交给 `SearchMemoryArchive` 读原文" |

**存量用户**：启动后在后台**自动回填**（分批、幂等、不阻塞加载）
- `facts.event_at/event_end` ← 由 `sources` 里的存档记录现算（纯 SQL）
- `records.speaker` ← `users` 里唯一的那位；否则从正文开头的 `[名字] ` 前缀取
- 日志：`[记忆·Z] 存量时间/发言人回填：N 条记录`

测试：默认 320 passed 10 skipped；KIRA_CORE 366 passed；web_audit 0。
新增 8 项：事件区间取自来源、合并保留整段区间、存量回填（时间+发言人）、
capture 按条记发言人、archive_view 暴露 speaker、提示词必须换算绝对日期、规则块字段说明、老库迁移。

### v2.13.5 (2026-09-12) — 「重整理事实」在工作台明细里能看见做了什么 📋

用户反馈：合并已经修好了，但点了「重整理事实」后**明细里什么都没有** ✗
（`rewrite` 任务只返回了 detail 文本，没写 `job_items`）

**修**（照「并入」那套设计）：
- 重做成功一条就写一条明细：`kind=fact`、`action=rewrite`、`before=` **重做前那条拼接内容**
  → 前端渲染成「**旧 → 新**」（旧的是拼接句、新的是重做后合并出来的），和「并入」同一套
- 随后真正的合并带着**同一个 job_id** 跑 → 明细里紧接着显示「并入」那几行，
  一次点击能把「还原了哪些来源、最后合并成什么」全看清楚
- 前端 `JOB_ACTIONS` 增加 `rewrite: "重做合并"`（任务卡片的 kind 标签仍是「重整理事实」），
  并把 `rewrite` 算进「旧 → 新」分支；它也不会被误标「已撤回」（目标随后会被并入别的事实）
- 自动重做（每轮审计那 3 组）没有 job，只打日志，不写明细 ✓

**验证**：假后端 + 真前端截图确认明细渲染正确（「重做合并」+ 旧 → 新 + 查看与编辑）✓
另加 2 项测试：手动重做必须写明细（含 `before`）、前端必须有标签且走「旧 → 新」分支。

测试：默认 309 passed 10 skipped；KIRA_CORE 355 passed；web_audit 0。

### v2.13.4 (2026-09-12) — 修「事实合并总是降级拼接」的真因：提示词与契约自相矛盾 ⚖️

用户点了重整事实后，日志里三组**全部**降级拼接：

```
事实合并模型输出不可用，改用原文拼接：structured_output_rejected ·
<field>.0: value_error; <field>.1: value_error; <field>.2: value_error
```

**根因**（我自己的两处矛盾）：
- **提示词**（`FACT_MERGE_PROMPT`）写的是「`source_ids` **至少一条**，逐字复制」
- **契约校验**（`FactMergeGroup.validate_action`）却要求 `len(source_ids) >= 2` ✗

两条事实合并时，模型按提示词只会给出"另一条"（1 条）→ 被我们判成输出不可用 → 降级拼接 ✗
而 `merge_facts` 本来就会自动把 `target_id` 并进组 → 这条 ≥2 规则既多余又自相矛盾 ✗

**修**：删掉这条 ≥2 校验（字段本身仍是 `min_length=1`，与提示词一致）。
另外把我们的校验消息加进可回显白名单，于是这类拒绝的日志会直接说清原因
（`<field>.0: value_error（merge requires content）`），不用再猜 ✗

**验证**：把旧规则塞回去，新加的测试立刻变红，并**逐字复现**上面那行线上日志 ✓

测试：默认 307 passed 10 skipped；KIRA_CORE 353 passed；web_audit 0。

### v2.13.3 (2026-09-12) — 重做机制不再依赖标记位（修「手动按钮识别不到」的真因）🔁

用户合并 v2.13.2 后反馈：**还是没有识别到那条拼接事实**。查到两个我自己的设计问题：

**① 挑选候选只看标记位** ✗
`rewrite_pending` 只在「启动时回填」和「本次降级拼接」两个时机被写上。
插件若只是**热重载**（或初始化没走到那一步），标记就是 0 → 手动/自动都挑不到 ✗
（主题修复是静态文件、刷新生效，容易让人以为 Python 侧也已经生效 ✗）
→ 现在**判据现算**：`pending_rewrites()` 直接查「这条事实**最新一条版本记录**的 reason
是否含降级拼接特征串」，标记位只作为附加条件。什么时候问，什么时候算得出来 ✓

**② 校验用了过严的 revision 计数器** ✗
原来要求「合并只 +1」，但**审计调 importance 也会 `revision=revision+1`**
（`storage.audit`）→ 明明内容没被动过，却被判成"被人改过"而拒绝重做 ✗
→ 改成用**内容**重建这一组：降级拼接 = `"；".join(内容)[:上限]`，于是按内容游标
贪心走一遍、每一步都对得上当前内容才算同组（顺序无关，被上限截掉的尾巴自然跳过）。
对不上就整体放弃 —— 宁可不重做，也不还原错的那几条。

**顺带**：拒绝重做时也会 `rewrite_attempts+1`，避免每轮审计都对同一条白试一遍。

测试：默认 304 passed 10 skipped；KIRA_CORE 350 passed；web_audit 0。
新增 2 项回归：**没标记也要挑得到**、**审计 bump 过 revision 也要能重做**。

### v2.13.2 (2026-09-12) — 重做机制对历史数据生效 + 主题不再被宿主桥改回去 🔧

用户实测报的两个问题：

**① 历史拼接事实挑不到**（手动按钮点了没反应）
v2.13.0 才引入 `rewrite_pending` 标记，所以**升级前**拼接出来的事实没有标记 → 手动/自动都挑不到 ✗
而且判据也找错了地方：降级拼接的 reason 只写在**版本记录**里（界面那行「最近审校」显示的就是它），
**不会**写到 `facts.reason` 上 ✗
- 现在启动时回填一次：按「这条事实**最新一条版本记录**的 reason」是否含「按时间拼接」来补标记
  （幂等：重做成功后会多一条「重做合并…」的版本，于是不再匹配）
- 另外老库的版本是**逐条 `time.time()`** 写的（差几微秒），按「同一时间戳」找整组会漏 ✗
  → 加了时间窗兜底，并用「这条内容确实被拼进了结果」过滤，避免把**别的簇**误当同组还原

**② 主题切黑夜后又被还原**
宿主把插件页放在 iframe 里，并**自动注入** `/plugin-bridge.js`；那个桥会按**宿主的**主题
反复设置 `<html data-theme>`（切侧边栏、宿主主题变化都会触发）→ 用户的选择被瞬间覆盖 ✗
- 现在监听 `data-theme` 变化：**用户明确选过 → 立刻抢回来**；**没选过 → 跟随宿主**（按钮图标同步，
  提示会写明「当前跟随宿主主题」）
- 浏览器实测：选黑夜后模拟宿主桥写入 light → 300ms 内恢复为 dark ✓；清掉选择则正常跟随宿主 ✓

测试：默认 300 passed 10 skipped；KIRA_CORE 346 passed；web_audit 0。

### v2.13.1 (2026-09-12) — 修 v2.13.0 的启动事故：老库升级失败 🚑

**现象**：合并 v2.13.0 后插件直接起不来 ——
`Failed to initialize plugin alife_memory_z: no such column: rewrite_pending` ✗

**根因**：v2.13.0 把「依赖新列的部分索引」建在了**建表脚本**里，而补列（`ALTER TABLE
facts ADD COLUMN rewrite_pending`）在**后面的迁移块**里 —— 全新库没事（建表时就带列），
但**老库**执行到 `CREATE INDEX ... WHERE rewrite_pending=1` 时那一列还不存在 → `no such column`
→ `initialize()` 抛错 → 插件初始化失败 ✗

**为什么测试没拦住**：当时的测试**全都建全新的库**，迁移路径一次都没跑过 ✗
（这正是「老库升级」和「新装」的分叉点。）

**修复**：把该索引移到事实列表迁移**之后**创建（并把这条规矩写成注释留在代码里）。

**新增 `tests/test_schema_upgrade.py`**（4 项）：先手工造一个**老 schema 的 facts 表**，
再跑 `initialize()`，断言
① 不抛错、② 两列被补上、③ 依赖新列的索引也建出来了、④ 老数据一个字不动、⑤ 重复 initialize 幂等。
**反向验证**：把索引挪回建表脚本里，这 4 项立刻全红 ✓（说明它真的能拦住这次的事故）。

数据安全：事故发生在 schema 阶段，**没有丢任何数据**；升级到本版本即可自愈。

### v2.13.0 (2026-09-12) — 降级拼接的事实可以重做了 🔁

**问题**：事实合并时如果模型输出不可用，会走降级"按时间拼接"（`engine.py:973-1005`）。
这条拼接记录**每句都真、也不是重复** → 合并流程再也挑不到它、审计也只判它"证据成立" →
于是"将就一次"变成"永久将就" ✗（卡片上那行「最近审校：模型输出不可用，按时间拼接」就是它）。

**为什么不是让审计改写那句话**：审计契约（`AuditAction`）只有 `keep / correct / merge / retract`，
**没有"新增事实"的动作** → 改写只能把 N 句压成 1 句、**会丢信息** ✗

**做法：把那次合并整个重做一遍**（信息一条不丢）
1. 降级拼接时给目标事实打 `facts.rewrite_pending=1`（其余行为完全不变）
2. 下一次审计轮询（每 2 小时）自动重做，每轮最多 **3 组**；也可用维护面板
   「重整理待处理事实」手动立刻排（手动不受次数限制）
3. 重做 = **还原**：来源从回收站取回、目标按合并前的版本快照回退 → 重新排进合并队列
   → 立刻合并一次（`merge_pending=1` 期间本来就不会被注入，所以没有"重复可见"的窗口）
4. 失败会自动重试，最多 **3 次**后停下（标记留给界面提示），避免反复烧模型调用

**界面**：事实卡片多一行提示（「待整理 N/3」＋说明），维护面板多一个「重整理待处理事实」按钮；
重做成功后卡片那行会变成「最近审校：重做合并：上次模型输出不可用，已还原来源重试」。

**保证**（都有测试钉住）：还原失败/来源被彻底删掉 → **整体放弃**，不做半截；
重复触发幂等；只碰带标记的行——重做前被人手工改过的（`revision` 不符）绝不动。
另修：`merge_facts` 同组版本现在共用同一个时间戳（原来逐条取 `time.time()`，
导致按「同一 reason + 同一时间戳」找回整组快照会漏）——重做就是靠这个把组找回来的。

**前端两处顺手修（同一 PR）**
- **主题选择会记住了**：黑夜/明亮按钮以前只改 `document.documentElement.dataset.theme`，
  既没存、也没在启动时恢复 → 刷新就回到默认 ✗。现在写入 `localStorage.alife-theme`，
  并在 `index.html` 的**样式表之前**用内联脚本恢复（先按上次选择，没选过就跟系统
  `prefers-color-scheme`），所以连开屏动画都不会闪一下；按钮图标也跟着变 ☀ / ☾。
- **开屏动画本来就适配黑夜**（背景、星点、文字、光环全走 CSS 变量），**只有图标上那道扫光
  写死了 `#ffffff96`** ✗ 暗色下白得刺眼 → 改成 `--sheen` 变量（亮色 `#ffffff96` / 暗色 `#ffffff33`）。
- 新增 `tests/test_theme.py`：主题必须持久化、恢复脚本必须在样式表之前、开屏样式里不许有写死的颜色。

测试：默认 291 passed 10 skipped；KIRA_CORE 337 passed；web_audit 0。

### v2.12.1 (2026-09-12) — 清洗后只剩空外壳的历史记录移入回收站 🗑

v2.12.0 的存量清理把 `<msg/>`（"这次不回复"）这类记录的内容清成了空串，**但记录本身还留在
列表里**——于是首页出现一张张只有时间戳和会话名的空卡片（实测截图确认）。它们没有信息量，
却照样占着 L0 席位、参与压缩阈值计数、进压缩输入。

现在存量清理多一步：**清洗后 content 与 summary 都为空的原始记录（L0、非永久）移入回收站**
（软删 + 写版本快照，写明理由，可一键还原），并新增口径版本 `meta.capture_scrub`
（= `2`），所以已经清过一轮的库会**自动再跑一次**把这批空壳收走。

**哪些会被收走（只剩外壳的）**：`<msg />`、`<msg></msg>`、纯空白。
**哪些一定留下**：只发表情（`[表情8]`）、只有引用（`↩123`）、图片/语音消息
（`[Image ...]`/`[Record ...]`）、工具结果——清洗后都非空，所以不会被回收。
新增测试专门钉住这条边界（`test_scrub_never_recycles_records_that_still_carry_information`）。

日志会分开报数：`存量记录清理：改写 N 条、移入回收站 M 条（只剩空外壳的原始记录，可还原）`。

### v2.12.0 (2026-09-12) — 默认更早整理 + 落库净化（外壳/思考块/空消息）🧹

**① 默认值：更早开始整理。** 原先是照抄 Alife 原版的 `100 / 70 / 0.4`，对短会话来说太晚——
**事实只在整理时提取**（工具的 `Memorize` 写的是常驻永久记忆，不是事实），所以攒不到 100 条
就一条事实都没有。现在：`threshold` **50**、`batch_size` **40**、`probability` **0.8**
（一问一答通常 2 条，带工具调用的回复会更多）。`batch_size` 必须小于 `threshold`，
差额就是压缩后留下的原文尾巴：**50 − 40 = 10 条**。
概率的三个检查点（Bot 每次回复后 + 调度器每 30 秒对所有会话各一次）意味着低于 1 也只是
晚几十秒，不会"永远不整理"。⚠️ 改的是**默认值**，已有配置不会被动，需要手动改或重置配置。

**② L0 落库前剥掉协议外壳。** 「外壳」指的是 `<msg>`/`<text>`/`<msg/>`/`</msg>` 这类
**进模型的那版文本**的容器——不是人说的话。代码里本来就有 `clean_text()`（`_strip_wrappers`），
但它只用在"给模型看的样子"上（压缩输入、工具读回），**落库路径没走**，于是 XML 外壳一路进了
原文、摘要和压缩输入。现在落库也走同一套清洗（`clean_text` 只吃标签、不吃标签外的字符，
正文里字面写的 `<text>` 原样保留）。各种写法都认：
`<msg> <msg/> <msg /> <msg attr="1"/> <MSG/> </msg> </msg > <text/> <text></text>`。

**③ 思考块不进记忆。** Agent 的 `<reasoning>…</reasoning>`（z 插件提示词里要求的推理标签）
原本被当成正文存进 L0，会被压缩进摘要与事实——例如「主人叫收声，立刻听话」这种内部指令
进了长期记忆就很糟。现在 Bot 的输出落库前先剥思考块：成对（跨行/带属性/大小写）、未闭合
（截到下一个 `<msg` 之前）、自闭合都认；**未闭合且后面没有 `<msg>` 时只删标签、正文一个字不动**
（宁可留下思考，也不丢正文）。

**③b 给模型的出口也一并收紧。** `model_text()`（压缩输入 / 工具读回 / 注入的统一出口）
与落库**共用同一套外壳规则**，所以这次所有标签修复对模型侧同时生效；此外它**比落库多剥一层
思考块**——落库保原文（用户有可能真的引用了 Bot 的思考块，那是他说过的话），但发给模型的内容
一律不带内部推理。这样即使存量清理没跑到，模型也不会被思考块污染。

**④ 没有实际发言就不建记录。** 只输出 `<msg/>`（就是 z 插件里"这次不回复"的写法）、
或渲染不出内容的消息，清洗后是空的——它们没有信息量，却会计入压缩阈值、白占压缩输入。
现在这类不再建 L0 记录（有工具调用的照记，只记工具摘要）。

**④b 工具返回也走同一套。** 工具感知结果落库前同样剥外壳（夹带的 `<msg>` 之类不该进记忆），空结果不建记录。

**⑤ 存量清理。** 升级后启动时会**自动跑一次**（只跑一次，记录在 `meta.capture_scrub`）：
L0 走落库同一套清洗，L1+ 的摘要**只剥思考块**（那是模型写的散文，避免误伤尖括号内容），
同步重算检索索引体。日志会报清理条数。

测试：默认 269 passed 10 skipped；KIRA_CORE 315 passed；web_audit 0。
新增 `tests/test_capture_hygiene.py`：空外壳的 22 种写法、正文安全性（`<msg_id>`、`3 < 5`、
字面 `<text>`）、思考块的 6 种形态、未闭合不吃正文、端到端落库、存量清洗幂等、默认值不变式
（含"schema 与代码两处默认值必须一致"）。

### v2.11.1 (2026-09-12) — 修三个索引问题：回填卡死 / 候选集不全 / 索引其实没被用上 🔧

合并 v2.11.0 后用户报：**界面一直显示「索引 回填中」**，同时 `on.im_message` 钩子刷 `AttributeError` 日志。
查下去发现 v2.11.0 的索引有三个问题（前两个会让它**看起来在工作、其实在漏召回**）：

1. **回填循环永不结束**（就是「一直回填中」）。根因：摘要为空 / 只有标点（算不出索引体）的记录，
   `search_body` 永远是 `''`，而 `''` 正是「还没进索引」的判据 → 每批都把同一批行捞出来，卡死并空转 CPU。
   现在这类摘要写占位符 `-`（FTS 分词器对它不产生任何词元，永不命中；这些行的词面得分本来就恒为 0），
   处理过就不再被捞出来。回填失败也不再留在 `building`（显式降级为 `unavailable` 并说明原因），
   成功则用 **info 日志报出补齐条数**——"到底好没好"一眼可见。
2. **候选集不是打分结果的超集**（真·漏召回）。索引体只有双字滑窗：单字查询「猫」查不到「…欢猫」；
   跨词边界子串「iph」查不到「iphone」。现在索引体收录**每个单字 + 每一对相邻字**，
   查询侧对 ≥3 字词元做同样的拆对 → 只要词元出现在摘要里，它的某个 n-gram 一定在索引里，候选集恒为超集。
   新增**子串全覆盖属性测试**：对一段文本的每个子串查询，逐一比对「索引路径 vs 全表路径」。
   索引体口径变了，存量库**自动重算**（`meta.search_index_scheme`，后台作废旧索引体再回填，不阻塞启动）。
3. **索引其实没被用上**（顺手查出来的）。SQL 里写的是 `(search_body='' OR rowid IN (…))`，
   SQLite 因为那个 `OR` 放弃 rowid 索引、退化成全表扫描：同一条查询 **0.28ms → 18ms**，等于白建。
   现在候选条件只保留纯 `rowid IN (…)`，"还没进索引的行"由 `_fts_hits` 直接并进候选列表
   （并给 `search_body=''` 加部分索引，那次探测从 18ms 降到 0.1ms）。

**顺带修**：① `on.im_message`（单条消息事件）里预热钩子直接崩——`KiraMessageEvent` 只有 `.message`
（没有 `.messages`），且**没有 `.sid`**（要走 `.session.sid`），等于预热一直算在 `""` 这个假会话上、永远命中不了缓存；
② 写入时**就地算好索引体**（原来 `capture` / 压缩归档 / 永久记忆 / 迁移导入都只写 `summary`，
新记录要等下次启动回填才进索引）；③ 编辑摘要 / 恢复旧版本时同步刷新索引体（否则那条会静默离开候选集）。

**实测（12,000 条存档，与全表路径逐条一致 ✓）**：`海龟汤` 64.9 → **17.7ms（3.7×）**；
`星月最近怎么样` 116.1 → **21.9ms（5.3×）**；`草莓蛋糕和咖啡` 136.5 → **21.9ms（6.2×）**；
命中过宽的单字查询自动退回全表（0.9×，只多几毫秒）。**修正 v2.11.0 的实测结论**：
当时那组数字主要来自打分下推，索引本身并没有真正生效（原因见第 3 条）。

测试：默认 222 passed 8 skipped；KIRA_CORE 266 passed；web_audit 0。

### v2.11.0 (2026-09-11) — 词面检索建 FTS5 索引（自动，存量用户零操作）🗂️

- **FTS5 索引**：汉字按 bigram 预切后建 FTS5 表（`trigram` 分词器对**双字中文词命中不了**，
  实测 `师傅` 匹配失败 ✗，所以不能用它），检索时先由索引取候选、再用同一套 SQL 表达式打分排序。
- **结果完全一致**：候选集是「打分 >0」的超集；未进索引的行（`search_body=''`，
  例如外部工具直接写库）**照常参与**，不会漏召回；命中过宽时自动退回全表。
  新增等价性测试：同一批数据两条路径结果必须相同（含绕过本模块写入的行）。
- **存量用户零操作**：启动时探测 SQLite 是否支持 FTS5（不支持就永久走全表，**零影响**）；
  支持则后台**分批回填**（不阻塞启动），完成后自动启用。
- **无新增依赖**：FTS5 是 SQLite 编译期特性，不需要 pip 包。
- **状态可见**：`/status` 增加 `search_index`（ready / building / unavailable），
  网页端「整理工作台」上显示「索引 已启用 / 回填中 / 不可用」小标签（**不加按钮**）。
- **安全**：MATCH 表达式一律按短语加引号、内部引号双写转义；索引前走同一个 `squeeze()` 归一化；
  索引是派生数据，异常可整表 rebuild。

**实测（12,000 条存档）**：`海龟汤` 77.5ms → **36.9ms（2.1×）**；`星月最近怎么样` 109.7ms → **43.9ms（2.5×）**，
结果逐条一致 ✓（4 千条时基本打平，交叉点在 8~10 千条）。

**顺带修掉一个静默召回损失**：词面检索原来有个 `[:24]` 词元上限，长消息会少召回 ✗ 现已取消。

测试：默认 213 passed 6 skipped；KIRA_CORE 253 passed。

### v2.10.0 (2026-09-11) — 召回提速：打分下推到 SQL + 并发 + 缓存 + 预热 ⚡

先用基准量出瓶颈（2 万条存档 / 1200 条事实）：

| 调用 | 改前 | 改后 |
|---|---|---|
| **词面检索**（最大头） | **1,221 ms** | **355 ms** |
| 事实词面匹配 | 65 ms | 22 ms |
| 其余（context / 常驻事实 / 名字） | ~100 ms | 命中缓存后 ≈ 0 |

- **根因**：`relevance()` 每被调用一次就**重新切一遍查询词元**，而它是注册进 SQLite 逐行回调的
  —— 2 万行 = 2 万次分词 ✗。现在把「词元命中 × 词长」的求和**整段下推成 SQL 表达式**
  （`instr` 纯 C 层），排序也用同一个表达式；**打分口径与原来完全一致**
  （有专门的等价性测试钉住：SQL 打分必须等于 `relevance()`）。
- **并发**：注入路径开头三个互不依赖的查询（上下文 / 名字保护表 / 实体解析）改 `asyncio.gather`。
- **进程内缓存**：只缓存「不随消息变化」的（上下文、名字保护表），按 store `revision` 失效
  （写入即失效，`touch_accessed` 不 bump，所以不会每轮抖动）。
- **预热**：消息一到（`on_im_message`）就预热该会话，把耗时挪到「打字 + 网络往返」期间；
  同会话 2 秒内只一次。
- **时间预算（默认关）**：`inject_budget_ms`，超过预算只跳过两个**可选通道**
  （跨会话存档检索、内容匹配召回），**核心召回一条不少**；默认 0 = 不限制，质量优先。
- **索引**：`facts(deleted,merge_pending)`、`records(permanent) WHERE permanent=1`。

实测真实注入路径（6000 条存档）：**394 ms（冷）→ 312 ms（热）**；用户当前规模（百条级）是 ms 级。

**仍未做的**：FTS5（`tokenize='trigram'`）能把全表子串扫描再降一个数量级，但需要建虚拟表 + 触发器同步 + 迁移，留作后续；向量路径（默认关闭）的逐行相似度回调同样待批量化。

测试：默认 212 passed 6 skipped；KIRA_CORE 253 passed。

### v2.9.3 (2026-09-11) — 整理范围与注入范围对齐（按归属会话）🎯

- **问题**：`recall_scope=global`（默认）时，**注入**的是**所有会话**的永久记忆（成本是全局的），
  而**整理**是按会话做的。于是膨胀发生在别的会话时：注入侧每轮都判定"超限"、每轮给**当前会话**排队，
  当前会话自己却没超 → tidy 跑完"没有需要调整的"，**全局超限状态永远消不掉**。
- **自动触发**：改成**按归属会话分别排队**（`{row["sid"] for row in perms}`），一次把真正膨胀的会话都排上。
- **手动整理**（`CorrectMemory(action=tidy)`）：范围**跟随 `recall_scope`** ——
  默认 global → 覆盖所有有意久记忆的会话；设为 `session` → 只整理当前会话；
  带 `ids` 时按那些条目的**归属会话**排队，并让它们立刻可被整理。

- **工作台按钮改为「整理所有会话」**（与 Bot 一致）：默认 `recall_scope=global` 时成本是全局的，
  所以按钮也按所有有意久记忆的会话排队（按钮上有 tooltip 说明；`recall_scope=session` 时仍是当前会话）。

- **永久记忆去重也跨会话了**：候选池跟随 `recall_scope`（默认 global → 所有会话），
  跨会话阈值更保守（**0.35**，同会话仍 0.25，配置 `permanent_dedupe_cross_threshold`）；
  每个簇交给「拥有最新那条的会话」处理，避免每个会话都对同一批簇重复调用模型；
  被并入的条目仍在各自会话归档，**明细里标出来源会话**（`【来自 绿岛酒吧】…`）。
  *（事实那边保持现状：跨会话合并仍只限 `profile / preference / relationship`，跨主体一律不合并。）*

测试：默认 211 passed 5 skipped；KIRA_CORE 251 passed。

### v2.9.2 (2026-09-11) — 前端更新后能真正生效（绕开 WebView 缓存）🔁

> 起因：合并 v2.9.1 后有人看不到新加的「整理永久记忆」按钮——代码里明明有。

- **带版本参数跳转**：资源指纹变化时不再用裸 `location.reload()`（安卓 WebView 会把缓存的
  `index.html` / `app.js` 原样再来一遍），改成 `location.replace(path + "?v=" + assets)`，强制取新的。
- **界面显示插件版本**：连接状态旁会显示 `已连接 · 实时同步 · v2.9.2`，
  一眼能确认前端是不是旧版——以后遇到「看不到新功能」先看这里。
- `/status` 增加 `version` 字段（读 manifest）。

测试：默认 209 passed 3 skipped；KIRA_CORE 245 passed。

### v2.9.1 (2026-09-11) — 修「手动整理永久记忆什么都没做」🐛

- **现象**：让 Bot「整理一下你的永久记忆」，她调用 `CorrectMemory(action=tidy)` 返回 `queued: true`，
  日志也立刻打「永久记忆整理完成」，但**其实一条都没看**（3 条永久记忆、上限 10、预算 3000 → 直接返回）。
- **原因**：`tidy_permanents()` 开头那道「必须超条数上限或字符预算」的闸门是给**自动触发**用的，
  却把**手动/委托触发**也挡住了。
- **修复**：闸门移到自动触发处（注入侧与定时器本来就已判断过）；**tidy 任务本身 = 「整理一次」**，
  不管有没有超上限都真的过一遍；条数上限触发时仍然一次排空积压。
- **顺带**：任务详情不再恒为同一句话——「该会话还没有永久记忆」「N 条都在整理间隔内」「本次没有需要调整的」
  会给出具体原因；新增回归测试锁住"容量未超时手动整理也必须请模型看一遍"。

- **事实合并软上限默认 100 → 80 字**（提示词里给模型的目标字数）；硬上限仍是 150 字。
- **硬上限触发时改为「带反馈重试」**：以前是原样再问一遍（模型很可能再超一次），现在会动态告诉它
  「上次有 content 写到 180 字，超过上限；请压到 80 字以内重写」——**目标值一律用软上限**，
  既不会再次撞墙，也给模型明确目标；reason 同理（软 15 / 硬 40）。

- **工作台新增「整理永久记忆」按钮**（与「压缩存档 / 审计与合并 / 合并相似永久记忆」并排）：
  走的是和 Bot `CorrectMemory(action=tidy)` 完全相同的链路；手动点不受容量闸门限制，真的会过一遍。

测试：默认 207 passed 3 skipped；KIRA_CORE 245 passed。

### v2.9.0 (2026-09-11) — 永久记忆不再"只进不出" + 召回去重 + 工具收敛到 4 个 🧹

**永久记忆整理**
- 此前永久记忆只会"相似合并"，攒多少都每轮全量注入。现在超过**条数上限（10）**或**字符预算（3000）**就触发整理，
  模型逐条给处置：`keep` / `extract`（提炼成事实后移出常驻）/ `archive` / `split`（约束留下、其余转事实）。
- 安全：**只归档不删除**（原文 + 版本 + 回收站都在，可一键取回）、提炼**只走既有事实链路**（不让事实库灌水）、
  幂等限流（同一条 14 天内不重复整理）、明细逐条可查。
- 入闸：`Memorize` 支持 `category` / `importance`；保存前先查"是否已被事实覆盖"，能覆盖就直接返回那条事实。
- 注入排序修复：永久记忆此前按入库先后排（**最新的反而最先被裁掉**），现在按
  「类别优先级（rule 最前）→ 重要度 → 时间」排，并从最不重要的开始裁。

**召回时触发去重**
- 本来就有「本地判定 → 只标记 → 后台模型合并」三段式，现在多了第 3 个触发点：**召回那一刻**。
  范围与阈值与写入侧完全一致（同主体 + 同类别 + 0.25），**不阻塞回复、不多花模型调用**；命中后当轮即不再重复注入。

**工具 10 → 4**
- `SearchMemoryArchive` 收下「按 id 读档」，`GetProfile` 收下「总览 + 名单」，
  `CorrectMemory` 收下「改称呼 / 刷新昵称 / 移出常驻」并扩展成全能力维护
  （`update` / `merge` / `delete` / `restore` / `archive` / `refresh` / `tidy`，全部要写 `reason`）。
- 工具 schema 从 ~4,934 字符降到约 4,200 字符/轮；更重要的是模型"该用哪个工具"的判断负担明显下降。

**向量融合（可选）**
- 启用 `semantic_enabled` 时，词面与向量改为**加权融合**（0.3 / 0.7），而不是"有向量就不看词面"；不启用则行为完全不变。

测试：默认 201 passed 2 skipped；KIRA_CORE 238 passed。

### v2.8.0 (2026-09-11) — 出站请求全面瘦身：压缩 / 审计 / 合并 / 每轮注入块 🪶🪶

v2.7.0 收拾的是「工具返回」，这一版把**所有发给模型的东西**都过了一遍。**QQ 号与用户 ID 一律不动**
（压缩质量优先，模型要靠稳定 ID 填 `subject`），省的全是"信封"。

- **压缩入参 −28%**：摘要剥包裹 + 折行空格合并；`role` 只在 assistant 时给 `bot:1`；
  时间从 `1772908601.6758957` 变成 `2026-03-08 02:36`（**带年份、本地时区**——以前模型要心算 epoch）；
  **`users` 每条内联名字**：`"qq:769690776(周武)"`（ID 在前，subject 照抄括号前那段）。
- **审计入参 −35%（来源多时 −49%）**：删掉 `sources` 字段（指令本来就要求模型别用它，
  服务端合并时从数据库行自己汇总）；事实 id 换成 `f1..fN` 短别名；证据时间改可读格式。
- **事实合并 −34% / 永久记忆合并 −30%**：同样的短别名 + 可读时间 + 摘要清洗。
- **`classify` 任务**：此前直接发原始数据库行（长 id + 浮点时间 + 内部字段），现已统一成压缩视图。
- **每轮注入块 −45%**：`facts` 用短键与类别短码（`c/u/x/imp/src/t`，图例写在静态规则块里，走缓存不花钱）；
  空字段、零值、默认重要度一律省略；`names` 改成映射式；`omitted_ids` 上限 30 → 10；
  跨会话来源直接给群名。**`subject` 保持稳定实体 ID 不变**，完整账号每轮仍出现一次（`names` 表）。
- **输出侧加约束**：`summary` ≤ 300 字、`scenario` ≤ 20 字、`content` ≤ 60 字（`reason` 保持 40 字）——
  摘要与事实会被反复注入，写长一次就每一轮都在付钱。
- **兜底归一化**：模型把 `qq:769690776(周武)` 整个抄进 `subject`、或只写了名字，服务端都会还原成稳定 ID。

实测（同一批真实记录）：注入块 960 → 521 字符；压缩 13,184 → 9,412；审计 5,551 → 3,604。

**追加（工作台）**：事实合并任务在前端一直显示英文 `fact_merge`、点「明细」是空的、日志里只有截断的 id。
现在：任务名显示「事实合并」；**事实合并与永久记忆合并都会写明细**（哪几条并进了哪一条，
前端渲染成「旧 → 新」，与事实审计同一套）；日志不再打 `id[:12]` 这种对不上的截断 id，
改成**短码 + 内容预览 + 中文任务名**；任务失败原因也带上异常消息（以前只给一个 `NameError` 这种类名）。

测试：默认 196 passed 2 skipped；KIRA_CORE 233 passed。

### v2.7.0 (2026-09-11) — 给模型看的东西瘦身 59%：短码 · 安全剥离 · 内容截断 🪶

起因是一段真实日志的成本复盘：工具返回一次 30 条要 9,268~12,211 字符，**其中只有 33.5% 是内容**，
其余是 id（16.6%）、账号数组（11.4%）、浮点时间戳（14.3%）和各种默认值字段（21.9%）；
两次检索就吃掉 10.1k tokens，直接把会话推到重开阈值（重开后缓存命中率 90% → 53%）。

- **证据编码改短**：真实 ID 有 31~71 字符（`legacy-<64 位十六进制>`），且抄错一位就查不到。
  现在对外统一给 **6 位短码**（落库持久化、重启不变），`ReadMemoryArchive` / `Forget` /
  `CorrectMemory` / `exclude_ids` 同时接受短码与完整 ID。
- **返回字段瘦身**：`i/t/s/u` 短键 + 默认值省略 + 账号数组换成昵称引用；
  **完整账号整批只给一次**（顶层 `who` 表），需要汇报或核对时从这里取。
- **安全剥离外层容器**：`<msg>`/`<text>` 成对时剥掉、消息边界保留；
  **正文里字面写的 `<text>`、`3<5` 原样保留**（锚定剥离，不数配对）。
- **长嵌套截断**：表情包的视觉描述（实测平均 276 字符/条，占摘要 31%）截到 100 字符，
  引用里嵌的原文截到 40 字符 —— 要看全文用 `ReadMemoryArchive`。
- **检索词空白归一**：日志里出现过模型传 `keyword='翅 膀'`（库里存的是「翅膀」）而检索不到。
  现在检索两侧（关键词过滤、词面打分、实体名匹配）都会归一化，被插进中文词里的空格不再影响命中。
- **真实空格有保护，不会误伤**：折行残留是**孤立的一个空格**（你库里 33 处实例全是这种）；
  连续被空白隔开的写法（`很 重 要`）视为刻意强调、原样保留；实体表里**名字真带空白的昵称**（`星 月`）
  会作为保护名单传入渲染，也会被实体匹配正确认出（用「星月」提问同样能命中「星 月」）。
- 每轮注入块同步瘦身：`src` 用短码、空的 `relations` 不再输出、`omitted_ids` 也用短码。

实测（同一批真实记录）：**22,582 → 9,256 字符（−59%）**；摘要侧 −36%
（剥包裹 1,141 字符 + 长描述截断 1,111 字符）。

测试：默认 184 passed 2 skipped；KIRA_CORE 220 passed。

### v2.6.0 (2026-09-11) — 被动召回更全：事实按内容匹配 + 归档也参与检索 🔍

- **事实支持内容匹配**：以前事实只能靠「类别常驻」和「提到某个名字」两条路进上下文——
  用户问「你师傅是谁」，而事实是 `relationship · 我师傅是星月`（主体和名字都没出现在消息里）时，就带不回来。
  现在消息内容会与事实内容做词面匹配（中文按 2 字切分），命中即带回，最多 `top_k` 条。
  新配置 `fact_recall_min_score`（默认 2）控制门槛，填 0 可关闭这条通道。
- **归档参与检索**：`search_active_only` 默认从「开」改成「关」。
  以前压缩后离开上下文的原文（历史归档）在被动召回里完全够不着——
  问「我最近在学吉他」可能一条都召不回；现在连归档一起搜，词面打平时**常驻优先**。
  冷归档、软删（审计撤回/合并）、超 180 天淡出依旧排除在外。
- **可观测**：DEBUG 日志会打印每条通道各带回多少条（`常驻 / 实体 / 触发词 / 内容匹配`），方便调门槛。
- 实测开销：单轮新增召回约 1–2 条事实、30–70 字符（预算 24000 字符）。
- 测试：默认 178 passed 2 skipped；KIRA_CORE 214 passed。

### v2.5.9 (2026-09-10) — 明细按钮真的能用了（属性名错位）+ 前端静态审计 🐛

- **现象**：点任务卡片上的「明细」，提示 `操作失败（404），请检查连接或登录状态`。
- **原因**：v2.5.8 把按钮属性改成 `data-jobdetail`，但处理器仍在读 `dataset.job`（= `undefined`），
  请求因此变成 `GET /job/undefined` → 后端查不到这个任务 → 404。上一版只改对了「写」的一半，没改「读」。
- **修复**：处理器改读 `b.dataset.jobdetail`。
- **同类问题全量排查**（其余均未再发现问题）：
  - 所有 `data-*` 属性的读写配对；
  - 每个 `$$("[data-x]")` 绑定与处理器**实际读取**的 dataset 键；
  - 前端调用的全部接口 vs `main.py` 注册的路由（路径 + 方法）；
  - POST 请求体字段 vs `contracts.py` 契约字段；
  - 动态重建容器的绑定时机（确认都在渲染后重绑，不会留下点不动的按钮）。
- **防回归**：新增 `tools/web_audit.py`（可单独运行，也被测试调用）+ 3 项测试。
  **把旧写法改回去，测试立刻变红**（已实测）。
- **端到端验证**：假后端 + 真前端，在浏览器里真点「明细」——修复后请求 `GET /job/job-abc123` 并正常弹出明细；
  还原旧写法则请求 `GET /job/undefined`，与用户看到的 404 完全一致。

### v2.5.8 (2026-09-10) — 修复手动任务按钮被「明细」覆盖 🐛

- **现象**：点「审计与合并」「合并相似永久记忆」等手动按钮，提示 `操作失败（404）`。
- **原因**：v2.5.6 给任务卡片加的「明细」按钮复用了 `data-job` 属性，而手动排队按钮也用 `data-job`；
  渲染后按属性统一绑定，把手动按钮的点击**覆盖**成了「打开明细」，于是去请求 `GET /job/audit` → 404。
- **修复**：明细按钮改用独立属性 `data-jobdetail`；两者各自绑定，互不干扰。
- **防回归**：新增 `tests/test_web_static.py`——检查 JS 里 `$("#id")` 引用的元素都存在于
  `index.html`、明细与手动按钮的属性不重复、样式表括号配对。

### v2.5.7 (2026-09-10) — 回收站页面（含取回/彻底删除）+ 明细里能看见撤回/修正的来龙去脉 🗂️

- **撤回的事实也能看了**：以前明细里只显示「不可读取」和理由——因为批量取事实时把已软删的过滤掉了。现在明细与编辑器都会带上已撤回的事实，内容照常显示，并标一个 `已撤回` 标签。
- **修正/合并显示方向**：明细里每条修正直接摆成 `旧内容 → 新内容`（旧内容带删除线）；合并会额外列出被并入的那几条，同样显示 `被并入内容 → 合并后内容`。
- **已撤回/被合并的事实可直接编辑与还原**：点「查看与编辑」能打开编辑器（顶部提示它已不在上下文中），在历史版本里点「恢复此版本」即可还原。
- **新增「回收站」页面**（侧边栏 `♺`，与概览平级）：按 **事实 / 存档 / 冷归档** 三个维度看已经不在上下文里的内容，支持按内容搜索、分页，每条都能「还原」（已删除的）或「查看与编辑」；还原动作本身也会写一条版本便于追溯。
- **冷归档可「取回上下文」**：把久未使用而冷处理的存档重新放回常驻与检索，同样留有版本记录。
- **彻底删除（高级操作）**：个别情况下确实想抹掉某条记忆，可以「彻底删除」——会连**版本历史与关联**一起移除、不可恢复，因此做了**二次确认**（先说明后果，再确认一次）。日常清理仍建议用「还原」。

### v2.5.6 (2026-09-10) — 任务明细可点开 · 流星全向 · 日志不重复 🌠

- **后台任务可以点开看明细**：压缩/审计任务卡片多了「明细」按钮，弹窗列出这次**具体处理了哪几条**——压缩显示产生的存档与被并入的原文，审计显示每条事实的处置（保留/修正/合并/撤回）与依据；每条都能直接「查看与编辑」，跳到已有的存档/事实编辑器。
- **流星方向改为全向随机**：以前固定往右下飞，现在从四面八方掠过（向右、向左、斜下、正下都有），只是不会从下往上。
- **日志不再重复**：完成任务只打一行，并且每种任务都带上有用的详情（`后台任务完成 audit（本次审计 20 条：保留 15 · 修正 3 …）`、`后台任务完成 compress（压缩 70 条 → L1）`、`后台任务完成 dedupe（常驻 2 条 · 合并 1 簇）`）。
- **明细只留近期**：任务明细保留 30 天，过期自动清理；写入频率很低（每次压缩/审计一次）。

### v2.5.5 (2026-09-10) — 星尘特效 · 事实内容搜索 · 审计明细 ✨

- **界面有了星尘**：背景一层缓慢闪烁的星点与低对比流星，前景一层偶尔掠过的亮流星；色相在同一色系里随机（蓝紫→紫→粉紫），顶部 `✦` 可一键关掉。跟随系统「减少动态效果」设置，切到后台自动暂停。
- **画像与关系页支持按内容搜事实**：搜索框旁勾选「按内容搜」，就能用「猫粮」「加班」这类词找事实（以前只能按实体 ID 精确过滤）；照常分页，筛选维度同样生效。
- **审计结果看得见**：后台任务从「审计了 20 条事实」变成 `本次审计 20 条：保留 15 · 修正 3 · 合并 2 组（并入 2 条） · 撤回 1`，日志同步打印。
- **配置项不再显示英文键名**：`/config` 直接返回 schema.json 里的中文名，前端优先使用——以后新增配置不会再漏。
- **「恢复默认」按钮**改成小药丸样式（11px + 细边框），比之前更克制。
### v2.5.4 (2026-09-10) — 主动感知节奏可控 · 通知不再污染记忆 ⏱️

- **首轮不再"重启即触发"**：主动感知第一次要等一个完整间隔（以前 `last_proactive` 初值是 0，插件每次启动都会立刻主动一轮）。
- **随机偏移 `proactive_jitter`**：实际间隔 = `proactive_interval` + 0~jitter 的随机秒数；默认 0 = 固定间隔。
- **每轮会话数可控**：`proactive_min_sessions` / `proactive_max_sessions` 决定每轮挑几个会话（0 = 不限，默认全部触发；两者相等就是固定个数）。
- **轮流挑选开关 `proactive_rotate`**：默认每轮纯随机；打开后优先挑最久没被触发过的会话，避免小概率饿死。
- **通知不再写进记忆**：主动感知、提醒类插件发的 `is_notice` 消息不再被记成"用户说的话"；Bot 的回复与工具调用照常记录（所以"bot 发了动态"这类行为仍然可被提取）。同时群聊通知的占位发送者 `unknown` 不再进入参与者列表。

### v2.5.3 (2026-09-10) — 上下文瘦身：情境化召回 + 请求体精简 💧

详见[上下文瘦身](docs/TOKEN_DIET_2_5_3.md)。实测（单会话 80 轮/天）：**每天约 501k → 103k token（-79%）**。

- **情境化注入（默认）**：常驻只放参与者、约定、偏好、画像和最近 1–2 条摘要；消息**提到某个人**就带回关于他的事实（含跨会话、跨用户），命中「记得/之前/上次」再放宽一档。实测注入块 16,227 → **1,993 字符**（-88%）。想回旧行为设 `inject_mode=full`。
- **事实只带必要字段**：去掉指纹、版本、合并标记、审计时间、场景、标签、内部时间戳；来源只留最新一条指针 `src`（跨会话事实另带来源会话 `sid` 与说话人 `by`）。`reason` 不再注入给对话模型，只留给审计和网页端。
- **人设开关**：`audit_persona` 默认关（审计只看证据，不需要语气）、`compress_persona` 默认开。人设动辄数千字，这是每次调用的大头。
- **审计新增 `retract`**：被证据推翻或纯属冗余的事实可以软删——立刻退出注入与检索，原文和版本都保留、可恢复。
- **请求体精简**：压缩请求用短别名 `r1..rN` 代替 UUID、去掉恒定的 `level`、L0 起止时间合并；审计只发判断需要的字段；JSON Schema 去掉 Pydantic 的 `title` 噪音。实测压缩请求 15,158 → **10,637 字符**，审计 14,196 → **9,162 字符**。
- **顺带修掉一个真 bug**：后台任务被取消时会把**已经完成**的任务改回 `queued`，导致重复执行（这是合并车道测试偶发失败的根因）。

### v2.5.2 (2026-09-10) — 存档按会话严格筛选 · 手动批量拉取姓名 🔎

- **存档页按会话严格筛选**：选中会话后只显示该会话自己的记忆。旧插件迁移的全局记忆、跨会话共享给本会话参与者的记忆，需要勾选「含跨会话记忆」才列出，并带 `跨会话` 标签（**工具侧的召回范围不受影响**）。
- **人物页新增「批量拉取姓名」**：点击后弹二次确认。「只补没有名字的」只处理缺名号码、已有名字一律跳过；「全更新」会用平台当前昵称更新已有名字，旧名保留在曾用名里、可一键恢复。
- **偏好设置文案字段加「恢复默认」**：压缩补充要求、事实/记录合并提示词等一键写回内置文案（改完仍需点保存）。

### v2.5.1 (2026-09-10) — 模型输出被拒时给出可修复的提示 🛠️

- **诊断信息带修复提示**：分类枚举、关系必须写在 `relations` 数组内、我们自己的语义校验原因（如「谓词没有表达具体关系」）都会写进重试反馈，**仍然不回显模型输出的内容**。
- **压缩提示词补输出格式速查**：8 个分类 + `relations:[{subject,predicate,object}]` 示例，减少写错枚举/摊平关系字段。
- **分类别名归一化**：`偏好 / Preference / general / 关系 / …` 自动映射到规范值（大小写不敏感）。
- **契约被拒第二次时缩小批次**：输出的事实更少，出错机会更小。
- 实测：用户报的那条 `facts.7.category: literal_error` / `facts.7.predicate: extra_forbidden` / `facts.10.category: literal_error` 在新版本下自动修正，只剩真正需要模型重写的语义问题。

### v2.5.0 (2026-09-10) — 记忆质量与成本 🧠

详见[记忆质量与成本](docs/QUALITY_2_5_0.md)。

- **写入时合并事实（借鉴 KiraOS）**：新事实入库时先和同会话、同主体、同类别的相似旧事实比对，达到阈值（默认 0.25）就**强制合并**——冲突以时间较晚的说法为准，模型输出不可用时按时间拼接兜底，绝不留下近似重复。身份/偏好/关系类允许跨会话合并为全局记忆。**迁移导入的旧记忆也会在导入完成后自动扫描一遍重复**（导入不走压缩，否则永远没人发现它们互相重复）。
- **合并完成前不参与检索**：检测到重复的新事实先隐藏，合并完成后才可见，避免对话里同时看到两条重复记忆。
- **审计节奏可控**：默认间隔 30 分钟 → **2 小时**；新增**冷却期**（同一条事实 7 天内不重复审）和**每日调用上限**（默认 24 次）。实测：2000 条事实的库，稳态从 96 次调用/天（约 50–110 万 token/天，且永不停止）降到只审新事实的水平。
- **老用户自动迁移**：检测到旧版本且该配置仍是旧默认值时，自动写入新默认（含提示词），用户改过的一律不动，只迁移一次。
- **事实重要度 `importance`（1–10）**：迁移时带入 KiraOS 原值，新事实由模型给出；永久记忆默认 8。用于画像摘要排序、检索排序与合并取值，审计也可按证据修正。
- **恢复历史版本**：记录、事实、名字历史都加了「恢复」按钮，恢复动作本身也写版本，可再退回来。
- **画像面板**：人物卡片直接显示画像摘要与统计，点开看按类别分组的事实、关系与名字历史；Bot 新增 `GetProfile` 工具，按名字一步拿到聚合画像。
- **模型命名更清楚**：`compress_model` → 「压缩与合并模型」，`audit_model` → 「审计模型」，避免字面误解。

### v2.4.0 (2026-09-09) — 原始对话不再重复注入 🪶

详见[原始对话不再重复注入](docs/NO_RAW_INJECTION_2_4_0.md)。

- **未压缩的最近原文（L0）默认不进每轮记忆块**：KiraAI 的上下文里本来就有这些对话，重复注入只会膨胀。实测（60 轮会话）注入块从 **22.6 KB 降到 0.3 KB**，与宿主历史的重复从 **100 条降到 0 条**；压缩后也从 9.8 KB 降到 0.46 KB。
- 需要时模型用 `SearchMemoryArchive` / `ReadMemoryArchive(id)` 主动检索；跨会话被动召回不受影响（其他会话的相关内容照常带入）。
- 新增配置 `inject_recent_raw`（默认关）可恢复旧行为。
- 顺带提升提示词缓存命中：注入块挂在最后一条消息里，块越小，每轮"未命中尾巴"越短。

### v2.3.1 (2026-09-09) — 迁移不再阻塞启动 ⚡

- 旧记忆迁移改为**后台执行**：`initialize()` 不再等待迁移完成，插件加载不被拖住；迁移期间记忆功能照旧暂停，完成后自动恢复。
- 源文件没有变化、且没有旧插件在运行时，启动时**跳过重扫**（只 stat 文件、不解析 TOML）；旧插件被重新启用或源文件有更新时会自动重新迁移。
- 迁移成功记录时间戳，失败不记录（下次启动继续重试）。

### v2.3.0 (2026-09-09) — 旧历史播种保护 🛡️

- 新增配置 `bootstrap_seed`（默认 `auto`）：首次遇到旧会话时是否把 KiraAI 已有历史抄进记忆库。`auto` 在检测到会话合并/压缩插件（KSM / ADS / Context Condensation）启用时**跳过播种**，避免把别的会话的对话记成本会话的经历；`always` 始终播种；`off` 从不播种。
- 播种状态改为显式记录（`meta.bootstrap:<sid>`），清理后不会重新播种，会话记录被归档清空也不会重复播种。
- 播种时写入「干净标记」`meta.bootstrap_clean:<sid>`（仅当时没有合并插件时），启动时只对**来源无法确认**的遗留记录提示核对：概览页提示条 + 「去清理」/「保留，不再提醒」，不自动删除。
- 后台任务页新增 **「清理历史播种记录」**：软删除安装时抄进来的旧对话（原文保留在版本快照里），并顺带清理只由这些记录支撑的事实。
- 文档重写：前言、机制、配置、兼容性与致谢。

### v2.2.8 (2026-09-09) — 合成昵称不再污染 🛡️

不再被第三方插件的合成昵称污染：跳过 `is_notice` 消息与「提醒任务所有者」等占位昵称，不再用它们更新人物/会话称呼；启动时自动检测并恢复被污染的名称为上一个正常称呼（后台任务页可手动重跑）。`MemoryNames` 返回同时瘦身为 `id/kind/name/revision/aliases`。

详见[不再被第三方插件的合成昵称污染](docs/SYNTHETIC_NAMES_2_2_8.md)。

### v2.2.7 (2026-09-09) — 开屏动画背景首帧即不透明 🎬

开屏动画遮罩去掉淡入：背景从第一帧就不透明，页面不会再从底下透出来（2.2.6 只把它改成了静态标记，但遮罩自身仍从 `opacity:0` 淡入 0.5 秒）。

详见[开屏动画背景首帧即不透明](docs/BOOT_OPAQUE_2_2_7.md)。

### v2.2.6 (2026-09-09) — 合并诊断与审计限流 🔍

永久记忆合并的任务详情现在写明原因：`常驻 N 条 · 相似簇 K 个 · 合并 M 簇 · 保留 J 簇`，并在没有可合并对象时说明「常驻不足 2 条（含已归档 N 条）」或「未发现相似簇（阈值 0.25）」。手动按钮与自动调度走同一逻辑，因此两边都能看到。

后台审计改为**每个间隔只处理最久未审计的 `worker_count` 个会话**，不再给每个会话都入队，导入大量旧记忆后也不会让后台一直满负荷；审计任务详情显示「本次审计 N 条事实」。

详见[永久记忆合并的诊断信息](docs/DEDUPE_DIAGNOSTICS_2_2_6.md)。

### v2.2.5 (2026-09-09) — 检测到相似就强制合并 🔗

相似永久记忆**检测到就强制合并**：以最新那条为基准，把其余各条独有的信息全部并入；模型若仍想保留，程序会用原文拼接兜底合并。新增开关 `dedupe_force_merge`（默认开），关闭则回到"只在确属重复或更正时才合并"的保守策略。

详见[检测到相似就强制合并](docs/FORCE_MERGE_2_2_5.md)。

### v2.2.4 (2026-09-09) — 同一会话只给新情报 🆕

同一会话只给新情报：搜索默认排除本会话已经给过的记忆（含每轮注入的存档与事实、按 ID 读过的存档），只返回新内容；`already_seen` 报告跳过数量，`allow_seen=true` 可显式重搜，`ReadMemoryArchive(id)` 永远能重读。`MemoryOverview` 的事实同样去重，每次最多返回 50 条新事实。同时删除写死的「还有别的吗」启发式（`re.fullmatch` 只认 8 个固定短语），改由模型通过 `SearchMemoryArchive(next_batch=true)` 驱动。

详见[同一会话只给新情报](docs/NEW_ONLY_RECALL_2_2_4.md)。

### v2.2.3 (2026-09-09) — 永久记忆去重与冷归档 🧊

永久记忆不再重复堆积：`Memorize` 遇到完全相同的永久记忆直接返回已有条目（被 Forget 过的会恢复），不新增；新增后台任务 `dedupe` 用**审计模型**检查相似的永久记忆，确属同一件事的重复或更正时自动合并——保留最新一条、其余移出常驻上下文（原文与版本保留）。开关 `permanent_dedupe`（默认开）与阈值 `dedupe_threshold`（默认 0.25）可在设置页热更改；后台任务页可手动触发。

**检索范围**：默认只搜常驻上下文，已归档的旧记忆需要显式 `include_archived=true` 才返回（结果带 `archived` 标记）；配置项 `search_active_only` 默认开启，关闭则恢复旧行为。对齐 Alife 原版"不翻旧账"的语义。

**冷归档**：被 Forget 或被合并取代的记忆立即冷归档；归档满 `cold_after_days`（默认 180 天）后自动冷归档。冷归档**任何检索都搜不到，只能按存档 ID 读取**，原文与版本全部保留，WebUI 可一键恢复常驻。

详见[永久记忆去重与自动合并](docs/PERMANENT_DEDUPE_2_2_3.md)、[检索范围](docs/ARCHIVE_SCOPE_2_2_3.md) 与 [冷归档](docs/COLD_ARCHIVE_2_2_3.md)。

### v2.2.2 (2026-09-09) — 载入动画与上下文卫生 ✨

新增打开面板时的载入动画：品牌图标旋转登场 + 标语 `ALIFE MEMORY / Z EDITION` + 每次随机的中文格言逐字浮现，点击可跳过；可在设置里热更改「是否播放载入动画」与「重播冷却秒数（默认 90，0=只播一次）」。同时修复一键拉取会覆盖用户已填名字的问题（占位实体解析到已有名字的目标时只做绑定、不写名字）；批量改为每批 20 个并显示进度、可随时停止，单次查询 10 秒超时且失败不写入；星图点击节点不再自动滚动页面；动效开关图标改为与侧边栏一致的 ❖。

**上下文卫生**：助手回复中的 `tool_calls` JSON 与工具结果不再整段进入摘要（只留 `[调用工具：…]` 与前 240 字预览，原文仍在 content），自身记忆读取不再被存成新记忆；启动时自动清理一次历史遗留的工具记录（软删回声、压缩摘要，原文保留）。**会话亲近度**：新增 `session_affinity` 开关（默认关），开启后全局召回按「同会话 > 同参与者 > 全局 > 其他」排序并标注来源，不缩小召回范围。

详见[批量拉取修复、载入动画与上下文卫生](docs/POLISH_2_2_2.md)。

### v2.2.1 (2026-09-09) — 压缩修复、批量昵称与星图交互 🛠️

修复分层压缩因混合可见性反复报 `ValueError` 的问题（迁移并入私聊的用户档案与实时消息分桶压缩）；失败任务现在显示可读原因。新增首次进入「人物与群名」页的一键批量拉取昵称（只补空缺、已填跳过、理由可追溯）。星图支持点击节点查看该实体的联结面板并筛选事实卡片，被淡化的连线不再可点，切换栏目自动回到 100% 缩放；节点名字按实体取色，并加入漂浮/呼吸/整图漂移与 ✨ 动效开关（可覆盖系统"减少动态效果"）。

详见[压缩修复、批量昵称与星图交互](docs/POLISH_2_2_1.md)。

### v2.2.0 (2026-09-09) — 身份合并与界面 🌐

迁移身份不再保留"旧插件档案"概念。`legacy:user:*` / `legacy:group:*` 按号码解析：优先并入同号码的现有实体，其次采用唯一在线适配器，仍无法确定时保留为 `unresolved:*` 占位并在之后自动绑定；`legacy:global` 与 `legacy:self` 分别成为独立的**全局记忆**与**机器人自身**桶，来源不明的内容归入**未分类**。同号码存在多个平台实体时不合并，只提示。人物与群名页可直接用 OneBot 查询裸号码，群聊与私聊分别使用群信息与陌生人信息接口。

侧边栏新增记忆星图缩放滑条，鼠标在星图内滚轮即可缩放；下拉筛选（会话、层级、维度）选择后立即生效，按钮只用于文本搜索；刷新默认自动并带旋转指示，点击一次切换为手动、再点一次刷新并恢复自动。整体加入同色系的过渡与悬停动效，可在系统"减少动态效果"下自动停用。

详见[身份合并与界面说明](docs/IDENTITY_2_2_0.md)。

### v2.1.1 (2026-09-07) — 可靠性修复 🔧

修复审计 ID 混用不重试、格式错误反馈不明确、同名审校原因丢失，以及"召回旧记忆又写成新经历"的重复回流。默认读取摘要和分页子存档，完整原文仍可按需读取；人物迁移区明确标注，审校原因在卡片直接可见。旧记录、原文与层级不重写，向量仍默认关闭。

详见[原因、边界、验证与主动/被动记忆说明](docs/RELIABILITY_2_1_1.md)。

### 更早

- 首次完整移植 Alife 分层记忆并重建实时编辑 WebUI；
- 结构化地基（6 类记忆类型、标签、来源、场景）、画像表、关系三元组；
- 安全迁移与互斥、固定系统缓存前缀、向量默认关闭；
- 中文 FTS5 检索修复（trigram 分词 + bm25 归一化）；
- 前端中央白条修复、召回并发加速、默认提示词回填。

</details>
